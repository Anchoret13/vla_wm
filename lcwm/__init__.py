"""pi05-lcwm: language-conditioned world model on a frozen pi0.5 backbone (LIBERO-100).

Import `lcwm.libero_paths` and call `ensure_project_libero_config()` BEFORE importing
`libero.libero` anywhere — hf-libero reads its config at import time.
"""
