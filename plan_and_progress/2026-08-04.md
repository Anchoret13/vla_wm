# 2026-08-04 — V7.3 execution record (contract lives in 2026-08-03.md)

This file is the single daily execution record for work performed on
2026-08-04 (America/Chicago). The governing preregistered contract is
the **V7.3 continuation in [2026-08-03.md](2026-08-03.md)** (sections
V7.3A–V7.3E, routing, and "Explicitly not doing in V7.3"); nothing
here amends it. Result roots created today carry the 2026-08-04 date
prefix per the registered rule (stage/run identity unchanged).

Standing registration discipline (2026-08-03.md): every training-path
stage is registered "EXECUTED — pending review" until its adversarial
review findings are fixed and re-verified; only then COMPLETE.

Status carried in: V7.3A COMPLETE after review (6 confirmed defects
fixed, contract re-PASS; commits c239cd9 → cf99caa → 07fd1b4f).

---
## V7.3B collector — review cycle closed; collection LAUNCHED

Review chain (per the standing discipline): 29-agent adversarial
review → **23 confirmed findings** (deduplicated: 12 substantive, 4
critical — order-dependent fidelity REGRESSION of the exact
historical bug, goal-blind privileged servo target, 7/8 branches
without stored normalized chunks, tau/time-axis corruption) → 12
fixes → 12-agent verification (9 green, 3 residual: tau aliasing via
inherited ABSOLUTE-timestamp prefix flips shadowing continuation
milestones; SHORTFALL rows polluting the budget tally; support report
at wrong granularity vs the registered cells) → round-2 fixes
(flip-timestamp rebase in cont_automata preserving damage semantics;
tally filter; per-anchor reversal sets + per-source pair sets +
per-anchor recterm contrast + explicit empty cells + cell checks) →
**3-agent re-verification: ALL GREEN** (commits 0629f01 → 48eb40f).

Frozen collection budget (in the run manifest): seeds 2400–2405+10k;
30 train + 10 dev blocker-targeted acquisition anchors (2/train
source, 1/dev source), 8 executed branches + u0 audit each,
closed-loop ≤3-decision recovery (atomic π0.5 first, goal-correct
privileged servo fallback, both attempts retained with traces and
videos), R=2 dual-goal unified-clock continuations,
recovery-to-terminal (≤300) for the canonical correction and the
MEASURED nonpositive sibling; 10 outcome-blind teacher banks
(generated, unexecuted). Status: EXECUTED — pending completion.

## V7.3B collection COMPLETE — honest support picture
(2026-08-04_v073_data_r1; RC=0; 33/33 acquisition shards, 422 videos,
9/10 teacher banks stored unexecuted; fidelity 33/33 order-independent
recompute, obj exactly 0.0, worst eef 6.6e-3.)

Anchors: 23/30 train + 10/10 dev + 9/10 teacher; 5 SHORTFALL rows
explicit (t2 one source 0/2; t4 three sources 0/2, 1/2, 0/2 and
teacher 1/2 — t4's dressing blocker rarely presents a reachable
window in fresh rollouts).

Recovery (125 attempts, all retained with traces + videos):
- atomic π0.5: canonical-goal positive at 2 anchors, alt-goal
  positive at 5;
- **privileged servo fallback: 0/59 positive** — within ≤30 actions
  even the privileged controller does not flip these blockers;
- 6/33 anchors have ≥1 positive recovery.

Registered cell checks (masks for V7.3C/D, applied as registered):
- milestone-positive canonical branches exist ONLY at t2|train
  (8/32 branches); every other cell is milestone-empty;
- **rank-reversal quota: unmet in ALL cells** — the action×goal
  reversal target family is MASKED project-wide this iteration;
- pos+nonpos source quota unmet everywhere (positives concentrate in
  one t2 source);
- recovery-terminal contrast present in ALL 10 cells (63+2 recterm
  runs at ≤300 steps).

Two firsts, recorded precisely:
1. **First terminal successes from a branch state in the project**
   (1300+ prior continuations all failed): BOTH at
   t2_s2410_d28 under the 300-step recovery-terminal horizon —
   rec_canon (atomic-π0.5 recovery, 113 steps) AND its measured
   nonpositive sibling c1 (155 steps). The pair is exactly the
   registered recovery-to-terminal contrast.
2. recterm q>0 in ALL five tasks (5/11/13/6/3 branches) — the
   300-step horizon reveals progress the 100-step horizon censored.

Per the bounded fallback: LCWM training, policy fine-tuning, and
LoHo proceed with the milestone/rank-reversal families masked where
unsupported; missing cells named in every downstream interpretation.
Status: collection COMPLETE (the collector's review cycle was closed
pre-run; no post-run code findings).
