# 2026-08-20 — Mainline reset: test counterfactual coverage, not terminality

## Status

- Work today: result review, claim correction, prospective action design, and
  then **Action 2P executed** — see the EXECUTED section at the foot of this file.
- New environment interaction: ~~**0 steps**~~ → **19,906 steps** (Action 2P).
- New GPU experiment: ~~**none**~~ → **Action 2P / V8.1P, registered HALT**.
- Action 1R is closed; its unused budget and unexecuted seeds are not reused.
- Action 2P was the next proposed experiment; its registration was sealed
  (`0979c839`) and independently reviewed (20 findings confirmed of 30, all
  fixed before execution), then executed. **Result: registered HALT.**

## 1. Objective checkpoint

The project objective is unchanged:

> Can a pretrained imitation-learned VLA improve from its own deployment failures by using targeted counterfactual interaction to refine a decision-relevant world model and distill better actions back into the policy?

The evidential sequence remains:

1. failure-anchored branches add action-effect and outcome support;
2. a learned WM acquires useful branches more efficiently than matched random/diversity selection;
3. WM-guided targets improve the final full-prompt `N=1` VLA beyond direct imitation of the same verified corrections;
4. a second bounded round moves the failure frontier and improves behavior beyond a matched-total one-shot update.

The benchmark band, task count, episode deadline, anchor rule, and value estimator are experimental instruments. They matter because they can invalidate a measurement, but they are not the scientific objective. V8.0 and Action 1R executed no sibling-action coverage experiment, no world model, and no policy update. Their outcomes therefore provide **zero direct evidence for or against all four method claims**.

## 2. What the 1R result does and does not say

### Registered/run status

Two statuses had been conflated on 2026-08-19:

1. **Run-level HALT:** the panel-2 duplicate subset spent `2,849/2,500`, a 349-step hard-cap overrun. This is the mechanical reason the run directory HALTed.
2. **No V8.1 handoff:** Action 1R never sealed a successor handoff. The registered last-progress rule yielded no admissible `chain1b` anchors in the existing readout, and Stage 1R.2 — the prospective source-cap stratum test — was never run. It is therefore incorrect to cite an unexecuted 1R.2 gate as if it had empirically proved that no earlier useful anchor exists.

The historical V8.0 fixed-deadline PASS remains the result under its original registration. The later evidence narrows its interpretation and withdraws its promotion eligibility; it does not rewrite the stored verdict.

### Scientific readout

| observation | supported interpretation | interpretation not supported |
|---|---|---|
| `chain1b`: success @250/@350/@500 = 8/20, 19/20, 20/20 | completion time is strongly deadline-sensitive; a 250-step miss is often recoverable and may provide a substrate for learning faster actions | “the policy never fails,” “there is no useful failure,” or “a late conversion is not a valid deployment failure” |
| `chain2b`: 31/40 @500; 34/40 @990 | performance changes with horizon; six rows remain unsuccessful at the maximum registered horizon | the six rows are terminal or irreducible |
| `chain2b` rows unsuccessful at 990 | three exact masks: 2 zero-progress, 3 tomato-done/cream-unresolved, 1 tomato+cream-pick/cream-place-unresolved | two strata, scalar `phase=2`, or ordered-prefix state identity |
| old last-progress anchors | the extractor placed `chain1b` anchors too late to observe the registered continuation before the deadline | earlier, pre-failure counterfactual actions cannot improve finite-deadline outcome |

The right terminology is therefore:

- `failure@L`: non-completion by a prospectively frozen deployment deadline;
- `right-censored@H`: not completed by the largest observed horizon;
- `retrospective_deadline_backoff`: an earlier history from a rollout that later fails the deadline.

Do not substitute `terminal`, `irreducible`, `stall_onset`, or `earliest_unrecoverable` without the corresponding evidence.

### Provenance carried forward

- Authoritative lifetime spend for Action 1R is **29,800 environment steps**, from `results/v080r_1r1/SPEND.json` and the ledgers.
- `POOLED_VERDICT_1R1.json` still embeds the stale panel-1-only value `15,596`; a successor note must correct the handoff without editing the historical artifact.
- Any new registration must seal the actual runner and dependency closure, external repository commit/dirty state, exact HF snapshot/weight hashes, BDDL/automaton, segment-level official versus diagnostic steps, and abort-safe cumulative accounting.
- Reserved `3200–3399` seeds remain untouched. Used `3400–3439` stay calibration-only; old-registered `3440–3479` are retired rather than reassigned.

## 3. Decision

We will not start another open-ended benchmark search, and we will not make “terminal failure” the next objective. We will directly test local feasibility required by the first rung of the method on the smallest setting that offers many finite-horizon failures **and** evidence that those outcomes can be improved in time.

The next action is:

> **Action 2P / V8.1P — finite-deadline sibling-coverage pilot on `chain1b@250`.**

This is a new, independently registered action. It is not an Action 1R rerun, so the old “change exactly one 1R component” restriction does not force the project back into benchmark repair. It is also not the full two-task V8.1 PASS.

The choice of `chain1b` is deliberate. Its 250-step result is reproducible at 0.40, so prospective sources contain many finite-horizon failures. Its late conversions show recoverability, which is useful rather than disqualifying when the immediate question is whether alternative VLA-supported actions can create a faster/better sibling. `L=250` is a post-calibration design choice that will be registered prospectively before Action 2P execution; every allowed claim is conditional on this experimental horizon. It is not presented as an exogenous deployment requirement.

## 4. Action 2P contract

### Question

At distinct, stratum-matched pre-failure histories from stock rollouts that miss `L=250`, do VLA-supported first-10-action siblings produce repeatable local action-effect/outcome variation, including both improvements and non-improving outcomes under the same continuation noise?

### Non-claims

Action 2P does not test:

- world-model prediction or model-guided acquisition;
- policy improvement;
- two-task or LoHo generalization;
- continual learning;
- unlimited-horizon competence or terminal-failure recovery.

It does not update a model or policy, and its rows are permanently excluded from later training and behavior evaluation.

### Frozen source and anchor

- Task/policy: `chain1b_lr2`, stock `pi0.5`, full prompt, deployment `N=1`.
- Horizon: `L=250`, a post-calibration experimental finite-horizon objective frozen before this pilot; it is not an intrinsic-capability horizon or external deployment requirement.
- Fresh source seeds: all `3480–3499`; run all 20, never early-stop at quota.
- Failure: valid non-success at step 250. A technical-invalid source triggers `TECHNICAL_HALT`, remains charged, and is not replaced or counted as a normal non-failure. Report both deadline failures `F/20` and exact-mask-eligible failures `E/20`.
- Anchor: fixed `tau=160`, because `160 + c=10 + H=80 = 250`.
- Literal anchor kind: `retrospective_deadline_backoff`.
- Exact eligible mask at `tau`: achieved/current-valid/damaged empty; pick actionable; pick and place unresolved.
- Selection: after the complete source panel closes, seal the first eight eligible failures in seed order. Underfill HALTs; failed replay is not replaced with the ninth source.

The source suffix establishes `failure@250` only. It is conditioned to fail and therefore cannot be used as a reference outcome.

### Candidate and execution design

For each of eight independent anchors:

1. after all sources and anchors are sealed, define reference `u0` as the first draw from a registered hash-derived stock-`pi0` RNG key, with no action-geometry or outcome selection and outside the `N=32` alternative draws; never use the source's cached post-`tau` action as reference;
2. draw exactly `N=32` raw stochastic `pi0` alternatives from separate presealed RNG keys;
3. deduplicate on the executed first `c=10` actions and require at least 16 unique alternatives;
4. select three farthest-point-diversity candidates using a preregistered standardized action feature/distance/start/tie-break, then select three disjoint matched-random candidates from the remaining pool;
5. globally hash-seal all eight pools, chunks, selections, execution order, and CRN schedules before any branch outcome is visible;
6. execute the reference and all six alternatives under the same three presealed continuation RNG keys;
7. after the ten-action prefix, replan with stock `pi0` and read outcomes after `H={20,40,80}` continuation steps; the ADVANCE decision uses `H=80` at deadline 250, while `H=20/40` and prefix effect are diagnostics only; never step officially beyond 250.

Each alternative therefore has three paired reference comparisons. Repeats are nested within an anchor; the independent scientific denominator is eight anchors, not 168 branches.

No proposal feature, pool selection, or tie-break may use the source trajectory after `tau`, its terminal mask/failure phase, or any branch outcome. The existing safe lexicographic contract supplies outcome components, priority, safety, and replay-noise floors; Action 2P replaces its best-of-three aggregation with same-key pairing. At `H=80`, a `paired-positive` must beat its matched reference on all three RNG keys. A `paired-non-improving` alternative never beats reference and is strictly worse on at least one paired key beyond replay noise. The exact deadline-outcome vector, directions, ties, and noise floors are sealed before execution. Reference variability and prefix physical effect by themselves do not count as candidate-induced deadline-outcome variation.

### Budget

```text
source:   20 × 250                         =  5,000
branches: 8 × (3 ref + 6 × 3 alt) × 90   = 15,120
hard N_env cap                           = 20,120
```

Every started or aborted environment segment is charged. There are no retries, substitute anchors, extra assessment branches, post-hoc quota increases, or budget reallocations. The ledger uses distinct `source`, `ref_prefix/ref_cont`, `div_prefix/div_cont`, and `rand_prefix/rand_cont` cap lines with source/anchor/candidate/CRN/policy hashes, actual official steps, and termination. Its worst case is 356 segments: 20 sources plus 168 prefixes and 168 continuations. A pre-segment headroom check prevents another execute-then-overrun failure. Candidate proposal count and GPU-seconds are reported separately.

### ADVANCE gate — not V8.1 PASS

All must hold:

1. all 20 sources close; every eligible failure is reported and exactly the first eight in the presealed order are selected; reserved-seed use is zero;
2. the same checkpoint and mask hash are restored for all 168/168 branch starts; all 24/24 reference prefixes meet the registered numeric replay tolerance;
3. every pool retains at least 16 unique first-10-action alternatives and executes the sealed paired schedule within the cap;
4. at primary `H=80`, at least 4/8 anchors show candidate-induced registered deadline-outcome variation;
5. at primary `H=80`, at least 2/8 anchors contain a paired-positive sibling; and
6. at primary `H=80`, at least two anchors each contain both a paired-positive and a paired-non-improving sibling.

Primary gates are reported per anchor at `H=80`; `H=20/40` are reported alongside them as secondary diagnostics. `diversity` versus `random` yield may be described, but the pilot is not powered or designed as a selector-efficiency comparison.

### HALT interpretation

HALT if any ADVANCE condition is unmet, including source/stratum underfill, provenance/replay failure, pool underfill, cap overrun, post-deadline official stepping, or failure of the joint positive/non-improving gate.

An outcome HALT means that this anchor/proposal/setting did not expose sufficient counterfactual support. It does not justify training or sweeping a world model. The next change must be one bounded anchor or proposal/setting revision, not a lower bar, a larger post-outcome budget, or indefinite benchmark shopping.

## 5. Handoff after Action 2P

- **ADVANCE:** register a source-disjoint second-setting replication, then build the complete two-task V8.1 bootstrap/handoff. Only replicated coverage can authorize training `M0`.
- **HALT:** revise one named coverage component and rerun a fresh prospective pilot, or stop the formulation if supported action diversity exists but repeatedly produces no useful outcome variation.

The strongest allowed ADVANCE statement is:

> On eight distinct `chain1b@250`, `tau=160` anchors selected from one prospectively registered event-mask stratum, the six outcome-blind stock-VLA siblings per anchor produced repeatable candidate-induced finite-horizon outcome variation on at least 4/8 anchors, a paired-positive on at least 2/8, and both a positive and non-improving sibling on at least two anchors.

It is not a WM, policy, benchmark-generalization, or terminal-recovery result.

## 6. Immediate action items

1. Publish a hash-bound 1R successor note with the authoritative 29,800-step spend, the three `chain2b` right-censored masks, and the run-HALT versus promotion-status distinction.
2. Implement the fixed-`tau=160` anchor extractor and exact-mask conformance test, including a zero-environment replay of the two aliased historical `chain2b phase=2` signatures.
3. Implement paired-CRN pool execution and the per-anchor dominance/variation reducer.
4. Add abort-safe segment ledgers and a pre-segment worst-case cap check.
5. Seal and independently review the complete Action 2P registration before any environment/GPU execution.

## Action status

| action | status after today's review |
|---|---|
| Action 0 | design/lineage complete; central formulation unchanged |
| Action 1 / V8.0 | historical registered fixed-deadline PASS; not a current V8.1 handoff |
| Action 1R / V8.0R | closed after Stage 1R.1 HALT; Stage 1R.2 not run |
| **Action 2P / V8.1P** | ~~next; prospective, registration not yet sealed~~ → **EXECUTED, HALT** (see foot of file) |
| Action 2 / V8.1 | paused pending Action 2P ADVANCE and a second-setting replication — **2P did not ADVANCE** |
| Actions 3–7 / V8.2–V8.5 | blocked on the corresponding upstream claim rung |

---

## Action 2P / V8.1P EXECUTED — registered **HALT**, with real positives
(2026-08-20; `results/v081p/2026-08-21T…`; registration `0979c839`, execution
seal `de5dedc5` written before any outcome existed; 20 sources + 168 branch
segments; **19,906 / 20,120** env steps; reserved seeds `3200–3399` untouched.)

### Provenance: clean, and the checks are no longer vacuous

| check | result |
|---|---|
| restore verified against a live re-snap | **168/168** |
| reference replay, max pairwise L2 across 3 repeats (tol `1e-3`) | **0.00e+00 on all 8 anchors** |
| pool uniqueness after first-`c` dedup (of 32) | **32/32 on all 8 anchors** |
| sources closed / eligible | `F = 11/20`, `E = 11/20` |
| anchors sealed, first eight in seed order | 3480, 3481, 3484, 3485, 3486, 3489, 3493, 3494 |

The prevalence estimate from the zero-environment test held: every one of the 11
deadline failures satisfied the exact mask at `tau=160`, against the 12/12
predicted. Replay is bit-exact, so the paired comparison rests on real CRN.

### The gate

| condition | required | got | |
|---|---|---|---|
| 4 anchors with candidate-induced variation | ≥ 4/8 | **3/8** | FAIL |
| 5 anchors with a paired-positive | ≥ 2/8 | **2/8** | pass |
| 6 anchors with both a positive **and** a non-improving sibling | ≥ 2/8 | **0/8** | FAIL |

Conditions 1–3 (sources/selection, restore/replay, pools/cap) all pass.
**HALT** on 4 and 6.

### What the eight anchors actually did

```text
a3480  var  ref succeeds 2/3 keys      div/rand mostly succeed;  1 non-improving
a3481  —    all 18 comparisons zero
a3484  —    all 18 comparisons zero
a3485  —    all 18 comparisons zero
a3486  —    all 18 comparisons zero
a3489  —    all 18 comparisons zero
a3493  var  1 paired-positive: rand1 [1,1,1] — and it CONVERTS, 3/3 success vs ref 0/3
a3494  var  3 paired-positives: div2, rand0, rand1 all [1,1,1]; 5 of 6 siblings beat ref on ≥1 key
```

Three findings, in order of importance:

1. **Positives are real, and one is a conversion.** At `a3493`, sibling `rand1`
   beat its matched reference on all three CRN keys and turned `failure@250`
   into `success@250` on **3/3** keys where the reference succeeded **0/3**. At
   `a3494`, three siblings are paired-positive; there the gains are in
   progress/timing, not conversion — no sibling there reached success.
2. **The binding failure is the absence of negatives, not of positives.**
   Exactly **one** paired-non-improving sibling exists across all 8 anchors, so
   condition 6 scores 0/8. The pilot was designed to require both, because a
   world model needs ranking signal in both directions; what it found is
   one-sided.
3. **Five of eight anchors are completely inert** — every one of 18 comparisons
   is an exact tie at `H=80`.

### The horizon diagnostic, and what the noise floors did

| horizon | anchors with **any** readout difference | anchors clearing the registered floors |
|---|---|---|
| `H=20` | 1/8 | — |
| `H=40` | 5/8 | — |
| `H=80` | **6/8** | **3/8** |

Differences accumulate with horizon, and the sealed floors are doing real work:
6/8 anchors differ in raw readouts while only 3/8 survive the quantization
floors. Reporting the raw 6/8 as "variation" would have passed condition 4 and
turned this HALT into an ADVANCE. The floors were derived from the automaton's
10-step evaluation stride and sealed before execution, which is the only reason
that call was not available to make after the fact.

### A design defect the data exposed, for the next revision

`a3480`'s **reference succeeds on 2 of 3 CRN keys.** Its source was a genuine
`failure@250`, so it passed the registered eligibility rule — but that source
failed under *its own* continuation noise, and at the same anchor the stock
reference usually succeeds. So the anchor is not a failure anchor under the
paired protocol at all, and its single "non-improving" sibling is a sibling that
merely succeeded less often than a mostly-succeeding reference.

Eligibility was defined by the **source's** outcome; the paired comparison is
against a **freshly drawn reference**. Those are different objects. A future
stratum should require the reference itself to fail on all (or a registered
majority of) CRN keys before the anchor is admitted.

### Registered interpretation

Per §4 HALT interpretation: this anchor/proposal/setting did not expose
sufficient counterfactual support. It does **not** authorize training or
sweeping a world model, a lower bar, a larger post-outcome budget, or benchmark
shopping.

Per §5 handoff, the two options are to revise one named coverage component and
rerun a fresh prospective pilot, or to stop the formulation if supported action
diversity exists but repeatedly produces no useful outcome variation. **The
second is not indicated by this evidence**: supported diversity plainly exists
(32/32 unique pools) and it does produce useful variation at 3/8 anchors,
including one genuine failure→success conversion under full paired CRN. One
bounded revision is the indicated path; which component is a registered decision
and is not taken here.

## Action status (updated after Action 2P)

| action | status |
|---|---|
| Action 0 | design/lineage complete |
| Action 1 / V8.0 | historical registered fixed-deadline PASS; `chain1b` band PASS void |
| Action 1R / V8.0R | closed after Stage 1R.1 HALT; Stage 1R.2 not run |
| **Action 2P / V8.1P** | **EXECUTED, registered HALT** (3/8 variation, 2/8 positive, 0/8 both) |
| Action 2 / V8.1 | remains paused; 2P did not ADVANCE |
| Actions 3–7 / V8.2–V8.5 | blocked on the corresponding upstream claim rung |

Environment interaction on 2026-08-20: **19,906 steps**, Action 2P only.
