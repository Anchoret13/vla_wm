# 2026-08-24 — Action 2M.4 executed: B_boot-v3 collected, M0.2 trained, held-out negative

Execution record for the item registered in [`2026-08-23.md`](2026-08-23.md)
§"ACTION ITEM — Action 2M.4". That file holds the mainline decision and the
contract; this one holds what was run, what came back, and what it cost.

**Action 2M.4 is COMPLETE** — the completion rule is that the fresh bank,
trained checkpoint, and held-out comparison all exist, regardless of sign.
All three exist. The sign is negative.

## Result — the test split, opened once

| model | physical MSE | effect MAE | effect RMSE | paired-effect err | top-1 regret | effect corr |
|---|---:|---:|---:|---:|---:|---:|
| **action-conditioned** | 0.001791 | 0.063241 | 0.097710 | 0.017362 | 0.016022 | **−0.2275** |
| state-only | **0.001298** | **0.036246** | **0.051979** | **0.014041** | **0.012647** | 0.0000 |
| stock-reference | — | — | — | 0.014041 | 0.012647 | — |
| train empirical mean | — | — | — | 0.014303 | 0.012647 | — |
| copy / no-change | 0.091660 | **0.014041** | — | — | — | — |

Calibration: damage 0.00256 vs 0.00044, success 0.02551 vs 0.00475 — the action
model is worse on both. Action-shuffle gap 0.01838 against the baseline's 0.0.

**The action-conditioned model loses to every baseline on every metric.** Three
readings that are each worth stating separately:

1. **The action path is used, and it hurts.** Shuffling actions moves the action
   model's loss by 0.0184 and leaves the baseline's at exactly 0. The model is
   conditioning on the action; conditioning is making it worse.
2. **Its candidate ranking is anti-correlated.** Effect correlation is
   **−0.2275**. It is not merely uninformative about which sibling is better —
   it is worse than uninformative, and top-1 regret is accordingly higher than
   just executing the reference.
3. **Neither model beats "assume every candidate equals the reference".** The
   copy/no-change baseline's effect MAE is **0.014041** against the action
   model's 0.063 and the state-only model's 0.036. Predicting the continuous
   consequence is harder than predicting no difference.

Note that `state_only` and `stock_reference` tie exactly on paired effect and
top-1 regret (0.014041 / 0.012647). That is by construction, not coincidence: a
model with no action input scores every candidate identically, so its predicted
delta is zero, which is the stock-reference predictor. The two registered
baselines are the same bar here.

Selection: validation chose epoch **4** for the action model and **27** for
state-only, before the test split was read. The action model peaks almost
immediately and then degrades — validation loss 0.90–0.96 at epochs 27–29
against the baseline's 0.49–0.69. With 48 training anchors and a target whose
signal-to-noise on the candidate mean is roughly 1.7×, it overfits the action
input fast.

## The readout change did what it was registered to do

This is the one clear success, and it is worth separating from the training
result. Action 2M.3 halted on a deterministic label that took **10 distinct
values across 900 branches**. The continuous `QΦ` target — the phase potential
evaluated at *every* environment step, discounted, averaged over shared-seed
repeats — took **80 distinct values across 80 branch rows** in the smoke, where
the legacy `G` vector took 11 on those same rows.

So the readout is no longer the binding constraint. The binding constraint has
moved to model and training, which is exactly where the completion rule sends
the next change.

## The bank

`results/v086_bank/2026-08-24T084023Z`, `groups.pt` SHA256 `c7126c59…`.

| | |
|---|---:|
| groups | **64** = 48 train / 8 val / 8 test |
| branches | **1,440** |
| sources drawn | 125 |
| eligibility yield | **0.512** (measured rate 0.538) |
| anchors from later-succeeding sources | **0** |
| interaction | **159,967 / 168,100** |
| restore failures | **0** |

Φ is evaluated at every environment step, its memos are built on the source
episode through `tau` and forked per candidate/repeat with the snapshot, and
nothing is rebased at the anchor. `scripts/test_v086_phase.py` proves the
forkable Φ is byte-identical to `episode_phase_potentials`, including that a
forked continuation reproduces the unforked episode tail.

## Two defects of mine, and what they cost

Both were in code I wrote for this action, and **both were caught by a number
looking wrong rather than by a test.**

### 1. The bank was anchored on the wrong population — 138,723 steps

The first collection released an anchor whenever the exact event mask matched at
`tau=160`, and never checked that the source episode **failed by step 250**.
Every prior action applies both conditions. **33 of 64 anchors came from
episodes that go on to succeed.**

The method is failure-anchored (framework §3), so training M0.2 on that bank
would have silently redefined the population the world model learns and broken
comparability with every earlier artifact — and it would not have shown up in
any training metric. The tell was the fill rate: 64 groups from 64 sources is
100% eligibility against a measured 0.538, and Action 2M.4's own ceilings
(108/23/23 for 48/8/8) are sized from that 0.538. The action item's arithmetic
contradicted my implementation.

Fix: release the anchor only when the source ends in `failure@250`. The snapshot
is still taken at `tau`; only the release moves to the deadline, where the
terminal outcome first exists.

### 2. A regression of my own cap fix — ~8,932 steps

The re-collection halted after three groups with `branch: worst case 80 vs
headroom 41`. I had built `SegmentLedger`'s quarantine list two actions earlier,
after Action 2M.3 attempt 2 hit exactly this failure — a superseded run's spend
eating its successor's cap. Then I invented a new label, `DISCARDED_`, that the
hardcoded list did not contain. **The list fails open.**

Fix: quarantine by regex on any ALL-CAPS label prefixed to a run stamp, verified
against an invented label to confirm a future one cannot slip through. The named
list is now documentation only.

### Cost

| tranche | steps | in a split? |
|---|---:|---|
| collector smokes | 10,620 | no |
| bank discarded — wrong anchor population | **138,723** | no |
| re-collection halted — cap regression | 8,932 | no |
| **`B_boot-v3` (kept)** | **159,967** | yes |
| **2M.4 total** | **318,242** | |

Project total across all V8 ledgers: **611,655** environment steps. Of 2M.4's
spend, **158,275 steps — 49.7% — bought nothing** and are charged.

## Registered handoff

Per the completion rule: M0.2 does **not** beat the state-only and
stock-reference baselines on held-out paired effect or top-1 regret, so the
matched-budget WM-guided-versus-random acquisition test is **not** unlocked.

> "If it does not, the next change concerns **model/training of an observed
> continuous target**; it does not return to another proposal, anchor, horizon,
> or noise-floor diagnostic."

The evidence localizes that change. The target is now continuous and
non-degenerate, the action path is demonstrably used, and the failure mode is
overfitting on 48 anchors — validation peaks at epoch 4 and then degrades, and
the learned ranking is anti-correlated rather than merely flat. That points at
capacity, regularization, or training-set size against a ~1.7×-SNR target, not
at the readout or the data-generating setup. **The specific change is not taken
here.**

## Status

| item | status |
|---|---|
| Action 2M.3 | COMPLETE — `NOT_IDENTIFIED` |
| **Action 2M.4 / M0.2** | **COMPLETE — held-out negative; action model loses to every baseline** |
| readout change | **succeeded** — `QΦ` is continuous where the label quantized |
| WM-guided acquisition | **not unlocked** |
| policy update | blocked |
| **Action 2M.5 / M0.3** | **READY / NOT STARTED** — reference-centered residual WM |

Environment interaction on 2026-08-24: **318,242 steps**.

---

## ACTION ITEM — Action 2M.5: reference-centered residual WM → M0.3

**Status:** `READY / NOT STARTED`.

**Objective:** train the world model on the causal object the sibling task asks
for — the consequence of changing the candidate relative to the reference — and
produce one fresh held-out sibling-selection result.

### One model change

M0.2 predicts absolute outcomes from absolute actions. In this local candidate
pool, most absolute-action variation identifies the anchor while the useful
signal is the small within-anchor difference. M0.3 uses the fixed factorization

```text
Y_hat(i)     = b(s) + Delta_hat(i)
Delta_hat(i) = h(z_s, u0, ui-u0) - h(z_s, u0, 0)
```

so `Delta_hat(reference)=0` by construction.

- Initialize `b(s)` and the state representation `z_s` from M0.2's selected
  state-only checkpoint and freeze them.
- Train one fixed 64-hidden-unit residual head `h` on reference-centered action
  inputs and candidate-minus-reference targets.
- Predict physical-effect deltas and continuous-consequence deltas directly;
  `delta_QΦ` is the sibling score. Retire the independent rank head — selection
  must come from the predicted world-model consequence itself.
- Use the existing `B_boot-v3` **48 train / 8 validation** groups. The previously
  opened test split is report-only and is not reused for checkpoint selection or
  the new claim.

### Fixed training

Fit delta normalization on train anchors only. Train for the inherited maximum
30 epochs with AdamW (`lr=3e-4`, `weight_decay=1e-4`) and select one checkpoint
by minimum anchor-balanced validation loss:

```text
L = 1.0 * SmoothL1(delta_QΦ)
  + 0.5 * SmoothL1(delta_physical)
  + 0.25 * SmoothL1(delta_auxiliary_consequences)
```

All targets are shared-seed candidate means minus the matched reference mean.
There is no per-repeat classification label, architecture sweep, regularization
sweep, or training-data recollection.

### Fresh held-out test

- Freeze source seeds `4200–4222`; take the first eight eligible
  `failure@250` exact-mask anchors.
- Reuse the unchanged stock-PI0 64-draw, reference-plus-four-max-spread candidate
  construction and six shared continuation keys.
- This is 8 groups and 240 branches under a **27,350-step** hard ceiling: 5,750
  source steps plus 21,600 branch steps.
- Seal the test bank before training and do not load its outcomes until the M0.3
  validation checkpoint is fixed.

Open the fresh test once. Report candidate-reference `QΦ` effect MAE and
correlation, physical-effect delta error, top-1 regret, and action-shuffle gap
against zero-delta/reference and M0.2.

### Deliverables and handoff

- versioned residual-WM model/trainer and mechanics test;
- fresh 8-group test bank with manifest and interaction ledger;
- `M0.3/best.pt`, complete `metrics.jsonl`, and one held-out `summary.json`;
- an execution result appended to the current-date document.

Action 2M.5 is complete only when the checkpoint and fresh held-out comparison
exist. If M0.3 beats both zero-delta/reference and M0.2 on paired-effect MAE and
top-1 regret, with positive effect correlation, proceed directly to the
matched-budget WM-guided acquisition test.
