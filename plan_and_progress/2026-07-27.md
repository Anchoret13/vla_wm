# 2026-07-27 — A-queue execution (A1 done, A2 code-complete)

Executes the review-to-action queue registered in 2026-07-25.md
(Review-to-action update — 2026-07-26). Sections below were briefly
appended to 2026-07-25.md and moved here to follow the dated-file
convention; content unchanged.

## A1 — registered before implementation

Acknowledged from the review: the v2/v3 "6/6" claim was overclaimed — the
Δ-space gate dropped mean-next, which wins on 6/6 sources in both runs, and
the EMA target's shared action input confounds the zero-action comparison.
Gate A stands FAILED; no fourth standalone run; full-state vs projected
becomes a P4 arm with mean-delta and shuffled-action baselines added.

## A1 design (registered)

**Phase-aware potential Φ** for the active atom (first false goal atom in
canonical order), lexicographic so no signal leaks across phases and raw
object-to-basket distance is used ONLY inside transport:

Φ = C + φ(active), where C = number of completed atoms (1.0 each; hard
no-damage: any completed atom turning false is a training-visible violation
flag, never silently re-scored), and φ ∈ [0,1):

| phase | entry condition (lexicographic, highest wins) | within-phase term | φ range |
|---|---|---|---|
| approach | default | 1 − clip(d(EEF, obj)/d_ref, 0, 1), d_ref = d at episode start | [0.0, 0.2) |
| grasp | d(EEF, obj) ≤ 0.05 m | gripper closure fraction | [0.2, 0.4) |
| lift | grasped AND obj_z ≥ z0 + 0.03 m | clip(Δz/0.10, 0, 1) | [0.4, 0.6) |
| transport | lifted AND moving | 1 − clip(dxy(obj, basket)/dxy_at_lift, 0, 1) | [0.6, 0.9) |
| placement | dxy(obj, basket) ≤ 0.08 m | descent into basket | [0.9, 1.0) |

Registered constants: near = 0.05 m (matches the contact trigger), lift
threshold 0.03 m, lift reference 0.10 m, basket radius 0.08 m; gripper
closed threshold to be calibrated on ONE train demo (task0 demo0) and then
fixed — calibration recorded here, never re-tuned per seed.

**Separate targets kept**: true one-block reward r (atom flips), phase
progress Φ/ΔΦ, composite success, environment termination, V^π0. The P5
score is registered as
Q̂_score = r̂ + λ_Φ·ΔΦ̂ + γ_decision·V̂(T(z,u)), λ_Φ = 1.0 default.

**Discount convention resolved**: γ_decision = 0.99 applied once per
ten-action decision everywhere (V targets, P5 score); the design doc's
γ^10 notation is interpreted as this single per-decision factor, not a
per-env-step discount compounded ten times.

**Exit check (registered)**: on task1 (cream-cheese/butter) demo episodes
— real pick-place arcs with labels — Φ must be non-decreasing up to each
atom completion on ≥90% of transitions, and specifically positive-ΔΦ
through grasp/lift windows where raw basket distance increases (the defect
that zeroed flow-positive coverage on 07-24). Hand-check script:
scripts/test_phase_potential.py, CPU-only on stored labels.

### A1 Amendment 1 (written before rerun): attachment replaces gripper closure

Hand-check trace (task1 demo0) falsified the registered gripper-closure
criterion: the summed gripper qpos at grasp is **object-dependent** —
soup can −0.012, butter −0.002, cream-cheese box **+0.003** — so no single
closed-threshold exists and `grasped` never fired on task1; Φ then
oscillated inside the grasp phase while the object was demonstrably being
carried (obj_z 0.446→0.731, d_eef pinned at 0.003). Replacement criterion,
object-independent and computable from stored labels: **attachment** :=
d(EEF, obj) ≤ 0.05 m AND ‖obj − obj_start‖ ≥ 0.02 m (the object has moved
with the hand). Grasp-phase progress becomes displacement fraction
clip(‖obj − obj_start‖ / 0.02); lift requires attachment + Δz ≥ 0.03 m as
before. Gripper constants stay recorded as calibration data but leave the
phase logic. Thresholds otherwise unchanged; the 90% exit condition and all
other registered terms unchanged.

### A1 result: hand-check PASSED (after Amendment 1)

`scripts/test_phase_potential.py` on stored labels (CPU): task1
(cream-cheese/butter) demos — 7 episodes, 181 windows, **Φ monotone 97.8%**
(registered bar 90%); of 36 grasp/lift windows, 29 have raw object-to-basket
distance increasing and **28/29 of those get ΔΦ > 0** — correct
manipulation is no longer labeled negative during grasp/lift, which was the
target defect implicated by the old **8/91** train flow-positive coverage
(7 groups × 13 branches); this hand-check does not remeasure branch-positive
coverage. No violation flags on successful demos. Chain episodes produce full
phase arcs (approach→grasp→lift→transport→placement) on their labeled prefixes.
Artifacts: `lcwm/phase_potential.py` (Φ, ΔΦ targets, violation flags,
BDDL-region resolution), `scripts/test_phase_potential.py` (calibration +
assertions). A1 exit condition met; A2 (full-episode replay cache + policy
interface) is next.

## A2 registration (written before implementation; build queued behind reboot)

Chosen bounded implementation: **(b) compact raw-observation cache** (not
replay-on-demand): the training loop must not depend on a live robosuite
env, the pattern is already established (contact groups store raw
`history_observations` per audit correction 5), and stored raw observations
allow prefix/KV recomputation under ANY instruction — which the crossed-
language losses need anyway.

`chain_episode_cache_v1` (per stock chain source episode, deterministic
replay with the recorded seed + per-decision noise; NOT new interaction):

- `prefix_hidden [71,968,2048]` fp16 + mask (full episode — recovers the
  ~304 post-soup+sauce states the loader currently misses);
- `raw_observations` per decision (2× 360×360 uint8 images + robot state,
  ≈55 MB/episode) — the P5 observation-side interface;
- executed actions env + normalized `[70,10,7]` and the full sampled
  50-step chunks `[70,50,7]` (exact stock chunks via recorded noise);
- labels q/obj_pos/bits captured live in the same pass and **asserted equal
  to the existing relabel sidecars at every decision** (free determinism
  regression);
- split from the branch manifest, unchanged; test-split source (1039) is
  built only behind an explicit flag and never auto-loaded.

Policy interface (`lcwm/policy_interface.py`): given a cached episode +
decision index, rebuild the obs dict → `_obs_to_policy_batch` →
`prefix_forward` → PrefixCache (hidden + KV) and sample N supported
candidates with recorded seeds.

**Exit test (registered, runs after reboot)**
`scripts/test_chain_episode_cache.py`: for a LATE-CHAIN decision (>45) of a
train episode: (1) regenerated prefix hidden matches the cached hidden
(fp16 tolerance); (2) candidate sampling returns [N,50,7] within policy
support; (3) the loader serves that decision as a labeled window with a
defined phase potential Φ; (4) split assignments unchanged vs the manifest.
Loader preference: full-episode cache replaces the branch-history prefix
for a chain source when present; falls back otherwise (no behavior change
until the cache exists).

### A2 implementation status: code complete, build queued behind reboot

Implemented and compile-verified; loader fallback confirmed unchanged
(P1 smoke test passes identically until a cache exists):

- `lcwm/chain_episode_cache.py` — full-episode deterministic capture
  (prefix, raw observations, exact stock 50-step chunks, live labels
  asserted equal to the relabel sidecars at every decision);
- `scripts/build_chain_episode_cache.py` — train+dev sources; test source
  only behind --include-test;
- `lcwm/policy_interface.py` — ReplayPolicyInterface: prefix/KV
  recomputation from stored raw observations under any instruction,
  seeded candidate sampling, prefix-exactness verification;
- `lcwm/seq_windows.py` — full-episode cache supersedes the branch-history
  prefix per source when present;
- `scripts/test_chain_episode_cache.py` — the registered A2 exit test
  (late-chain coverage >= 250 labeled 2/3 states, Φ defined late-chain,
  prefix exactness, [N,50,7] candidates, splits unchanged).

Post-reboot sequence (one command each):
`python scripts/build_chain_episode_cache.py` then
`python scripts/test_chain_episode_cache.py`. A3 (P4 mixed slice) follows.

---

## Codex review — mainline entry (2026-07-27)

### Result interpretation

The A1 result is real and useful, but narrow. An independent rerun reproduces
the registered task1 numbers exactly: 177/181 windows are non-decreasing
(97.8%), and 28/29 basket-distance-increasing grasp/lift windows receive
positive ΔΦ. A small held-out-demo sanity check also remains healthy (task0
47/49, task1 47/47 non-decreasing windows). This establishes that the new
potential repairs the known target-semantic defect on successful pick-place
arcs.

It is **not** yet evidence that an LCWM learned action-conditioned dynamics,
ranks alternative actions, or improves π0.5. The 181 windows come from seven
correlated expert episodes, Amendment 1 was motivated by inspecting task1
demo0, and failure/recovery branches have not yet been relabeled with ΔΦ.
Accordingly:

| item | reviewed status | interpretation |
|---|---|---|
| A1 target semantics | **DONE** | shaping target passes train + small held-out expert-arc sanity |
| A2 replay/policy interface | **CODE-COMPLETE; EXIT PENDING** | cache does not exist yet; exactness/candidate test has not run |
| A3 grounded mixed P4 | **NOT STARTED** | no new model-learning or candidate-ordering result |
| A4 model-guided FM + paired N=1 | **NOT STARTED** | the previous one-seed N=1 tie remains the latest policy result |
| A5 targeted interaction | **CONDITIONAL** | no measured trigger yet |

The planned `>=250` late-chain states are coverage across only seven
train/dev source episodes, not 250 independent samples. Metrics and confidence
must continue to aggregate by source episode.

### Bounded contract repairs inside A2/A3

These are integration requirements for the mainline run, not a new diagnostic
phase:

- [ ] Build `chain_episode_cache_v1` and pass the registered A2 exit test before
  treating A2 as done. Keep the test split sealed.
- [x] Make the replay-fidelity statement match the implementation. The builder
  now asserts q, object pose, predicate bits, and exact sidecar length.
- [ ] Wire the A1 target into P4 explicitly. The current
  `ValueHeads` now exposes separate predicate-fraction and `dphi_hat` outputs;
  the remaining A3 work is to construct and consume the registered ΔΦ target
  in the mixed trainer.
- [x] Define the single canonical model score as
  \[
  \hat Q_{\mathrm{score}}
  =
  \hat r
  + \lambda_\Phi\widehat{\Delta\Phi}
  + \gamma_{\mathrm{decision}}\hat V(T(z,u)),
  \qquad
  \gamma_{\mathrm{decision}}=0.99,
  \]
  rather than the stale \(\hat r+\gamma^c\hat V\) form. A3/A4 consumers must
  call the canonical `q_score` helper.
- [x] Separate composite success, environment termination, and time-limit
  truncation in the cache/loader; derive chain success from goal predicates.
- [ ] Include the registered smallest paraphrase/compatible-goal consistency
  batch in A3. Cross-language recomputation is part of the project contract,
  not an optional later diagnostic.

### A3/A4 scientific contract

The failed standalone P2 Gate A does **not** block launching A3. Do not run a
fourth latent-rank experiment. Train the mixed sequential + sibling-branch P4
slice and let grounded outcomes arbitrate the representation.

For a checkpoint to support the LCWM claim, however, the **same checkpoint**
must establish both:

1. recursive closure beyond copy, zero-action, shuffled-action, and mean-delta
   baselines on held-out source episodes, without collapse; and
2. grounded q/task-object effects plus candidate ordering beyond
   progress-only, action-marginal, and effect-only controls.

Latent rank alone neither promotes nor blocks a checkpoint. A promising
grounded checkpoint should receive the cheap paired full-prompt `N=1` A4
readout immediately; that readout need not wait for a perfect multi-step model.
But a policy gain without recursive evidence is policy adaptation, not evidence
for the intended language-conditioned predictive-state mechanism.

### Mainline decision

Proceed on the existing queue:

```text
A2 artifact + exit test
        ↓
A3 mixed grounded world-model training
        ↓
A4 model-generated first-ten FM targets + paired N=1
        ↓
A5 only if A3/A4 measures a concrete coverage failure
```

No additional standalone target, latent-rank, q/gripper, or generic branch-data
diagnostic is required before A3. The next scientifically meaningful result is
whether a learned language-conditioned recursive state produces calibrated
action consequences and then changes the deployed `N=1` π0.5 policy.

---

## Response to Codex mainline review (same day)

Interpretation accepted in full: A1 is a target-semantics repair on seven
correlated expert episodes — not LCWM evidence; the 8/104 figure is
corrected to 8/91 (their edit, kept); A2 is not DONE until the registered
exit test passes; the ≥250 late-chain states are coverage over seven source
episodes and all metrics keep aggregating by source.

Bounded contract repairs implemented now (CPU-verified, GPU still blocked
by the NVML mismatch):

1. **Replay-fidelity claim = implementation**: the cache builder now
   asserts q AND object poses (atol 1e-4) AND predicate bits at every
   decision, plus an exact sidecar-length check; divergence aborts the
   build.
2. **Honest progress heads**: `p_hat` is renamed in semantics to predicate
   fraction (`predicate_fraction_target`); a fifth head `dphi_hat` is added
   whose target is exactly `phase_potential.delta_phi_targets` — the ΔΦ
   the P5 score consumes.
3. **Single score implementation**: `value_heads.q_score` implements
   Q̂ = r̂ + λ_Φ·ΔΦ̂ + γ_decision·V̂ (γ=0.99, λ_Φ=1.0); the stale
   r̂+γ^c·V̂ form is superseded and every consumer must call this helper.
4. **success/termination/truncation separated in the loader**: chain
   success is derived from the goal predicates per record; a record is an
   environment termination only when the episode terminated — truncation
   can no longer become a positive termination label;
   `episode_terminated`/`episode_truncated` are explicit fields.

Deferred to their proper places: cache build + A2 exit test (first GPU
command after reboot); paraphrase/compatible-goal consistency batch (A3, as
the review places it); the A3/A4 same-checkpoint contract (recursive
closure incl. shuffled-action and mean-delta baselines AND grounded
effects/ordering) is adopted as the promotion rule for the P4 run.

CPU regressions after the repairs: P1 loader smoke test PASSED unchanged;
value-heads emit 5 heads and the canonical score composes.

---

## Mainline action items — executable queue

The queue below replaces open-ended diagnosis. Every step must preserve the
project chain:

```text
language-conditioned recursive state
        → grounded action consequences
        → model-generated policy targets
        → better deployed full-prompt N=1 π0.5
```

Candidate ranking is a calibration test and a source of training targets, not
the final system.

### A2.1 — Materialize and close the replay interface

- [ ] Restore the GPU stack and confirm `nvidia-smi` before launching a model
  process.
- [ ] Build `chain_episode_cache_v1` for the registered train/dev sources only;
  do not pass `--include-test`.
- [ ] Run `scripts/test_chain_episode_cache.py`.
- [ ] Record, by independent source episode:
  - number of cached episodes and decision records;
  - number of post-soup+sauce records;
  - maximum prefix-regeneration error;
  - sampled candidate shape and seed;
  - sidecar q/object/bits agreement;
  - unchanged source split assignments.

**Artifact:** cache manifest plus one A2 exit-test record in this file.

**Exit condition:** a late-chain state can be served both as a labeled
world-model transition and as an exact π0.5 policy input with reusable prefix
KV and seeded `[N,50,7]` candidates. Only then mark A2 DONE.

### A3.1 — Assemble the mixed P4 training contract

- [ ] Extend the sequential-window target builder with separate:
  - one-block atom reward;
  - predicate fraction;
  - ΔΦ and no-damage violation;
  - composite success;
  - environment termination;
  - time-limit truncation;
  - valid-only \(V^{\pi_0}\).
- [ ] Compute ΔΦ with the episode history/memos required by
  `episode_phase_potentials`; do not infer a phase from an isolated endpoint
  pair.
- [ ] Relabel every existing sibling branch with the same ΔΦ/no-damage
  semantics used by sequential episodes.
- [ ] Preserve the physical targets separately:
  - absolute Δq;
  - absolute task-object displacement;
  - sibling-centered q/object effects;
  - predicate flips.
- [ ] Add the smallest crossed-language batch from existing observations:
  - canonical instruction + one same-goal paraphrase, with invariant semantic
    targets;
  - one compatible alternative goal where its BDDL predicates can be evaluated,
    with the physical future shared and task-relative targets relabeled.
- [ ] Keep every paraphrase, relabel, branch sibling, and physical source in the
  source episode's existing split.
- [ ] Expose the cached original 50-step π0.5 chunks for demo rehearsal, while
  world-model action credit remains on the executed first-ten block.

**Artifact:** one `p4_mixed_v1` loader/manifest smoke record printing aligned
state, action, next-state, all masks/targets, language variant, behavior policy,
and source ID. This is data plumbing over existing interaction, not a new
dataset-collection phase.

**Exit condition:** sequential and sibling samples enter one trainer without
target overloading or split leakage, and `dphi_hat` is supervised by the same
ΔΦ consumed by `q_score`.

### A3.2 — Train the grounded LCWM vertical slice

- [ ] Keep the pretrained π0.5 trunk/action expert frozen and the policy adapter
  at stock no-op for the first world-model run.
- [ ] Train the current recurrent `LCState` and grounded heads jointly on:
  - one-step recursive prediction;
  - absolute and sibling-centered physical effects;
  - reward, predicate fraction, ΔΦ, no-damage, success/termination;
  - valid \(V^{\pi_0}\);
  - paraphrase invariance and compatible-goal physical consistency.
- [ ] Compare at most two matched representation arms inside this same P4
  setup: full predictive state and the smaller learned prediction projection.
  Do not launch another standalone P2/rank run.
- [ ] Select checkpoints without looking at the sealed test source.
- [ ] Report held-out metrics aggregated by source episode:
  - recursive error versus copy, zero-action, shuffled-action, and mean-delta;
  - q/task-object effect error and resolved-source pass fractions;
  - reward, ΔΦ, success, and \(V^{\pi_0}\) calibration;
  - paraphrase invariance and compatible-goal physical consistency;
  - candidate pairwise accuracy and top-1 regret versus progress-only,
    action-marginal, and effect-only-v3 controls.

**Artifact:** one reproducible P4 checkpoint/config plus held-out source-level
evaluation. Report failures as results; do not repair them with an unregistered
seed or threshold sweep.

**A4 trigger:** send every checkpoint with nontrivial grounded candidate
ordering to the cheap A4 action-shift/N=1 readout. A strong paper-level LCWM
claim still requires the same checkpoint to show recursive closure beyond the
trivial baselines.

### A4.1 — Turn model predictions into π0.5 training targets

- [ ] At cached train states, sample a fixed seeded candidate set from frozen
  π0.5 and score only each candidate's executed/replanned first-ten block with
  the canonical `q_score`.
- [ ] Calibrate score ordering and positive margin on real train/dev sibling
  branches before using predicted advantages.
- [ ] Convert conservative positive advantages into clipped first-ten
  flow-matching weights; never reverse-imitate a negative candidate.
- [ ] Freeze or slow the world model during this first distillation step and
  retain full-chunk demonstration rehearsal.
- [ ] Train the required matched policy arms:
  - stock π0.5;
  - GT branch-FM;
  - LC-Flow with matched capacity but no world loss/targets;
  - effect-only v3;
  - LC-Flow v0.4 with model-guided FM.
- [ ] Measure first-ten action shift from stock before spending a closed-loop
  rollout. If the adapter produces negligible change despite a calibrated
  model score, test action-expert PEFT as the next bounded capacity change.

**Artifact:** distilled adapter checkpoints, candidate-score calibration, and
the pre-rollout action-shift table.

### A4.2 — Read out the actual policy endpoint

- [ ] Run paired full-prompt Chain3 `N=1` with matched environment seeds and
  policy-flow noise wherever architectures permit.
- [ ] Report SR, Q, per-atom completion time, first unresolved atom, and failure
  phase for every arm.
- [ ] Run the registered small LIBERO-10 retention slice before promoting a
  checkpoint.
- [ ] Attribute the result correctly:
  - full method beats stock and no-world control → evidence for the intended
    world-model-to-policy path;
  - full method ties no-world control → policy adaptation, not WM evidence;
  - ranking works but action shift is negligible → policy-interface/capacity
    failure;
  - action shifts but Q/SR does not improve → score calibration or candidate
    support failure.

**Exit condition:** the experiment reaches the deployed `N=1` policy, not only
offline ranking or best-of-N.

### A5 — Collect only against a measured mainline failure

- [ ] Do not collect generic branches after A2.
- [ ] Trigger `16–32` targeted late-chain source episodes only if A3/A4 shows:
  - π0.5 samples a useful action but the model cannot rank it;
  - uncertainty/ranking failure concentrates after soup+sauce;
  - nominal recursion works but alternative-action effects lack support; or
  - the updated policy leaves the cached state distribution.
- [ ] After one bounded collection batch, return directly to A3/A4; do not open
  another collection-centered project loop.

### Immediate next command

The only operational blocker before the mainline run is the GPU driver/library
mismatch. After the reboot/module repair:

```text
build A2 cache → pass A2 exit → build p4_mixed_v1 → launch A3 P4 run
```

There is no remaining action item to further validate A1 on successful expert
trajectories before entering A3.

---

## A3.1 registration (written before implementation)

Target builder (`lcwm/p4_targets.py`), seven separated target classes per
window, one documented source each — no overloading:

| target | source | availability |
|---|---|---|
| r (atom delta) | bits_t → bits_{t+1} | all labeled windows |
| predicate fraction | bits_{t+1} | all labeled windows |
| ΔΦ + no-damage | `episode_phase_potentials` WITH episode memos | labeled windows whose full prefix history is labeled |
| composite success | bits.all per record | all labeled windows |
| env termination | terminal ∧ episode_terminated | never from truncation |
| truncation | episode-level flag | explicit, separate |
| V^π0 | MC atom-return, π0 episodes only | generating-policy-guarded |

Demo-episode termination derivation (registered): LIBERO ends on success,
so env_terminated := (first_success_t is not None); otherwise the episode is
action-exhausted = truncated.

**Branch sibling ΔΦ (memo rule per review):** Φ memos are accumulated by
running the potential over the PARENT source episode's labeled prefix
(sidecar labels) up to the snapshot decision; each sibling's after-state
(q_after, object_pos_after, bits_after) is then evaluated as the alternative
next record. Contact-group snapshots lie on the pure rollout → ΔΦ defined.
Stall-group snapshots lie AFTER unlabeled recovery segments → memos
unavailable → their siblings' ΔΦ is masked unavailable, never inferred from
an isolated endpoint pair. No-damage per sibling from bits_after vs
bits_before.

**Compatible-goal predicates (registered choice):** exact BDDL evaluation
via a small deterministic relabel replay (queued with the A2 GPU batch) —
no geometric In() approximation. Paraphrase prefixes likewise need the
frozen trunk → GPU batch. The CPU-side A3.1 deliverable is the target
builder + branch ΔΦ + `p4_mixed_v1` smoke; language batches join when the
GPU returns.

### A3.1 CPU-side result: target builder + smoke PASSED

`lcwm/p4_targets.py` + `scripts/test_p4_mixed.py` (CPU, real artifacts):

- demo episode: 37/37 windows labeled, ΔΦ valid on all, env_terminal
  exactly 1, no V^π0 (expert guard holds);
- chain episode (partial prefix, pre-A2-cache): 28 labeled windows, ΔΦ on
  the labeled prefix, 29 V^π0 targets, **no terminal semantics emitted**
  (partial prefixes carry none — full-episode semantics arrive with the A2
  cache, where truncated_final=1 is then asserted);
- branch contact group: 13/13 siblings get memo-rule ΔΦ, 0 violations;
- branch stall group: 0/13 valid — masked unavailable by construction,
  exactly per the review's memo rule.

Honest note: at the seed-1010 snapshot all 13 sibling ΔΦ saturate at 0.392
(within-phase clip caps once displacement ≥ 2 cm) — ΔΦ separates
effective-vs-stalled coarsely but does not rank among uniformly effective
siblings; fine-grained ordering must come from r/V and the physical effect
heads in `q_score`. Recorded, not tuned.

Remaining before the A3.2 launch: A2 build + exit test (GPU), crossed-
language batches (GPU), then the mixed trainer.

---

## A2.1 result: cache built, exit test PASSED — A2 DONE

GPU restored (RTX 5090, NVML OK). Build: 7/7 train+dev episodes, 71 records
each, **sidecar-checked=True on every episode** (q + object pose + bits
asserted at every decision + exact length); test source 1039 refused as
sealed. Registered exit test PASSED:

- **304** labeled 2/3-complete states across train+dev (review estimate
  ~304; per source: 1000:43, 1001:34, 1010:45, 1011:47, 1012:43 (train);
  1018:45, 1038:47 (dev));
- **prefix regeneration max|Δ| = 0.000e+00** at a late-chain decision
  (bitwise; from stored raw observations through the frozen trunk);
- candidates [4,50,7] sampled at that state (seed 777050);
- split assignments unchanged vs the branch manifest;
- late-chain phase records defined (approach — consistent with the stall).

Loader upgrade verified: train transitions 500 → **735**, dev 145 → **236**;
chain episodes now 70/70 labeled windows with 70 V^π0 targets each.

Two defects the smokes caught and fixed during integration (recorded):
(1) my loader repair had conflated the terminal POSITION marker with the
termination flag, erasing truncated_final — the marker now passes through
and the termination/truncation split stays in p4_targets; (2) the builder
did not record length-cap exhaustion as truncation — builder fixed for
future builds, loader derives it for the existing cache (truncated :=
truncated ∨ ¬terminated). After both: p4_mixed smoke fully PASSES
(chain: truncated_final=1, env_terminal=0; branch memo rule unchanged).

**A2 DONE.** Next: crossed-language batches (paraphrase prefix recompute +
exact-BDDL compatible-goal relabel) and the A3.2 mixed grounded run.
