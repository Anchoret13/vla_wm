# 2026-07-29 — Close the model-to-policy loop before architecture changes

**Status note.** Phase I is now complete. The original A/B/C sections below
are retained as preregistration and execution history; the current action queue
starts at [Active supersession](#active-supersession--matched-attribution-and-long-horizon-performance).

## Decision

The current architecture should not be expanded before the first complete
model-to-policy evaluation. `LCState` already provides an action-conditioned
recurrent belief, an explicit controlled transition \(T(z_t,a_t)\), and a
direct path from the learned state to the deployed policy.

The immediate objective remains unchanged:

```text
learned recurrent predictive state
        → action-learning signal
        → changed π0.5 policy
        → improved full-prompt N=1 success
```

The current evidence sets two experimental priorities.

1. The existing `v0.4-e2e-v1` path must first be completed as a fixed
   vertical-slice baseline. The current results do not yet evaluate the main
   hypothesis because no P4 world model has produced a policy checkpoint.
2. The next strong baseline is a simpler recurrent belief-conditioned policy
   trained from reliable action supervision, without model-generated candidate
   ranking. This tests whether recurrence and direct policy conditioning alone
   explain any gain before additional representation engineering.

Do not add a new feature encoder at this stage. π0.5 already provides both
task-aware late prefix features and earlier visual features that can be used if
the current representation must be revised.

---

## Current evidence

| item | current interpretation |
|---|---|
| recurrent state and π0.5 adapter wiring | mechanically validated |
| P2 recurrent self-prediction | did not pass Gate A; mean-next remained stronger on dev |
| full chain cache | complete |
| actionable late-chain train decisions | 207, but from only five train chain sources |
| P4 world-model checkpoint | none |
| model-generated teacher | none |
| new policy checkpoint | none |
| deployed-policy evidence | old one-seed 0/0 tie; not a test of the current mainline |

The three current risks are:

1. **Self-referential latent targets.** The late prefix and its EMA copy may
   admit a slowly varying, low-rank solution that does not preserve
   action-dependent future differences.
2. **Low-bandwidth policy conditioning.** Four latent tokens are mean-pooled
   into one global AdaRMS bias, which may be insufficient for phase- or
   action-token-specific corrections.
3. **Late-stage support.** The 207 decisions are highly correlated within five
   chain trajectories, and successful final-stage continuation has weak
   support. Candidate ranking may therefore be dominated by the manually
   specified \(\Delta\Phi\) term rather than learned continuation value.

These are failure hypotheses to localize after the first closed-loop run, not
reasons to delay that run.

---

# Phase I — Complete the existing `v0.4-e2e-v1` loop

## A0 — Correct the launch-invalidating value target

- [ ] In `lcwm/p4_targets.py`, change the transitioned value target from
  `episode_value_targets(episode)[:-1]` to `[1:]`.
- [ ] Assert transition-level alignment:
  target \(t\) equals \(V^{\pi_0}(s_{t+1})\).
- [ ] Assert zero terminal next-state value.
- [ ] Assert that
  \(\hat r_t+\widehat{\Delta\Phi}_t+0.99\hat V_{t+1}\)
  does not reuse the current-state value or double-count the current reward.

**Exit:** the assertions pass on one real chain episode.

## A1 — Train one fixed P4 world model

- [ ] Implement `lcwm/p4_train.py` and `scripts/train_p4_e2e.py` using the
  existing cached sequential and branch data.
- [ ] Run the two-step plumbing smoke and strict checkpoint reload.
- [ ] Train only `p4_e2e_v1_wm`, seed 0, with the fixed 30-epoch budget already
  specified in the 2026-07-28 plan.
- [ ] Do not run an architecture, loss-weight, or representation sweep.
- [ ] Save model, optimizer, EMA, source manifest, config, and per-loss logs.

Offline metrics are recorded but do not block the policy stage.

## A2 — Produce the model-generated teacher

- [ ] Generate the fixed `N=4` candidate set for all 207 actionable late-chain
  train decisions.
- [ ] Score the first ten executed actions with
  \[
  \hat Q_i =
  \hat r_i + \widehat{\Delta\Phi}_i + 0.99\hat V_{\mathrm{next},i}.
  \]
- [ ] Use the highest-scoring non-stock candidate only when it scores above
  the cached stock candidate.
- [ ] Save deterministic candidate seeds, selected chunks, recurrent states,
  checkpoint hashes, and score components.

The teacher artifact must additionally report:

- positive-teacher fraction by source and decision time;
- predicted advantage distribution;
- candidate action diversity relative to stock;
- the separate contribution of
  \(\hat r\), \(\widehat{\Delta\Phi}\), and
  \(0.99\hat V_{\mathrm{next}}\) to each selected advantage.

These quantities are diagnostics for failure routing, not promotion gates.

## A3 — Train and evaluate the current AdaRMS policy adapter

- [ ] Freeze π0.5 and the P4 world model.
- [ ] Train `wz_hidden/wz_out` with model-guided first-ten flow matching and
  full-50 stock rehearsal using the fixed five-epoch budget.
- [ ] Measure first-ten action shift on a fixed set of late-chain states:
  normalized \(L_2\), cosine similarity, and per-action-dimension change.
- [ ] Evaluate stock and `v0.4-e2e-v1` on paired full-prompt Chain3 `N=1`
  rollouts using seeds `1048, 1058, 1068, 1078, 1088`.
- [ ] Report success count, completed atoms, per-atom completion time, first
  unresolved atom, failure phase, and action shift.

**Required artifact**

```text
results/lc_flow_v04_e2e_v1/
  world_model/
  candidate_teacher.pt
  policy_adapter/
  paired_n1_5seed.json
  teacher_diagnostics.json
```

No new architecture work begins before `paired_n1_5seed.json` exists.

---

# Phase II — Test the simpler recurrent-policy explanation

## B0 — Build `pi05_belief_bc_v1`

This arm tests whether recurrent history and direct belief-conditioned policy
training improve π0.5 without learned candidate ranking.

- [ ] Reuse the same `LCState` recurrence and the same π0.5 policy interface.
- [ ] Use only reliable expert or observed positive-branch action targets.
- [ ] Train the policy with direct flow-matching/behavior-cloning supervision.
- [ ] Retain predicate, progress, and physical-effect auxiliary supervision for
  the belief, but do not use \(\hat Q\), candidate selection, or predicted
  advantage to construct policy targets.
- [ ] Match the policy capacity, optimizer budget, rehearsal data, and
  evaluation protocol of the Phase-I policy arm.
- [ ] Evaluate on the same paired five-seed full-prompt `N=1` protocol.

## B1 — Add one matched recurrence control

- [ ] Replace the recurrent history with a current-observation-only state while
  preserving the state dimension, policy adapter, trainable parameter count,
  action supervision, and training budget.

The first comparison is therefore:

```text
stock π0.5
vs current-observation policy adaptation
vs recurrent belief-conditioned policy adaptation
vs model-guided recurrent policy adaptation
```

Interpretation:

| result | supported conclusion |
|---|---|
| recurrent belief beats current observation, but model guidance does not add improvement | history-dependent policy adaptation is useful; the learned world-model teacher is not yet supported |
| model-guided arm beats matched recurrent belief adaptation | learned predictive scoring provides additional policy improvement |
| neither recurrent arm changes actions | the current policy interface is the primary bottleneck |
| actions change but neither recurrent arm improves behavior | action supervision, representation, or late-stage support is the primary bottleneck |

Do not describe a `belief-BC` gain by itself as evidence that controlled world
model prediction improved the policy.

---

# Phase III — Conditional architecture and data changes

Only enter the corresponding branch after Phases I and II localize the failure.

## C1 — If action shift is negligible: increase policy-side capacity

1. Train action-expert Q/V LoRA or equivalent narrow PEFT using the same
   Phase-I and Phase-II targets.
2. Apply matched PEFT capacity to the model-guided and direct-supervision arms.
3. Re-run the paired `N=1` comparison.
4. Only if PEFT still cannot use the belief, insert two to four projected
   belief tokens into the action-expert suffix with a zero-initialized residual
   gate.

Do not introduce belief tokens before testing the less invasive PEFT path.

## C2 — If actions change but model scoring is ineffective: revise the state

Build `LC-Flow v0.5` using π0.5-native feature factorization:

- physical belief tokens updated from early π0.5/SigLIP visual features,
  proprioception, and executed actions;
- task/progress belief tokens updated from the physical belief, late
  PaliGemma prefix features, and language;
- two physical and two task tokens initially, preserving the current total
  latent-token count;
- stop-gradient next early-visual/proprioceptive/effect targets in addition to,
  rather than only, the EMA recurrent latent target;
- policy access to both physical and task tokens.

The intended factorization is:

\[
\bar w_t=T_w(w_{t-1},a_{t-1}), \qquad
w_t=U_w(\bar w_t,V_t^{\mathrm{early}},q_t),
\]

\[
g_t=U_g(g_{t-1},w_t,H_t^{\mathrm{late}},l).
\]

Here \(w_t\) represents action-relevant physical state, while \(g_t\)
represents task-relative progress. Language need not alter physical dynamics;
it determines which physical distinctions are relevant to the task-conditioned
state and policy.

## C3 — If teacher support is weak: add targeted branch data

Trigger this branch when positive teachers are rare, candidate advantages are
uniformly non-positive, or selected rankings are dominated by
\(\widehat{\Delta\Phi}\) with little contribution from learned continuation
value.

- [ ] Collect 16–32 targeted late-chain branch sources around the unresolved
  final atom.
- [ ] Include successful, near-success, and physically distinct failed
  continuations from matched starting states.
- [ ] Split by source state/episode, not individual transitions.
- [ ] Retrain the same Phase-I model before changing architecture.
- [ ] Increase candidate support to `N=8` once only after the model has
  relevant outcome support.

More candidates cannot repair a score model that has not observed the
relevant action outcomes.

---

## Immediate queue

```text
A0: fix V_next alignment
        ↓
A1: implement and launch one fixed P4 run
        ↓
A2: generate N=4 teacher + score decomposition
        ↓
A3: train current AdaRMS adapter + paired five-seed N=1 evaluation
        ↓
B0/B1: recurrent direct-supervision baseline + matched no-recurrence control
        ↓
route to C1, C2, or C3 using the observed failure mode
```

The next progress update should contain at least one of:

- a running `p4_e2e_v1_wm` job;
- a completed `candidate_teacher.pt`;
- a new policy checkpoint;
- `paired_n1_5seed.json`.

It should not add another standalone latent diagnostic or a new external
encoder before the current model-to-policy path has been evaluated.
---

# Execution log (Claude)

## A0 result: value-target alignment fixed, assertions PASSED

`p4_targets.sequential_targets` now emits `v_pi0` = V^π0(s_{t+1}) (index
`[1:]`; the current-state value is kept separately as `v_pi0_current` for
the MC-identity check). Assertions on the real seed-1000 chain episode, all
passing: (1) `v_pi0[t] == value_target_mc(bits, t+1)` at t ∈ {0, 30, last};
(2) exact MC identity V_t = r_t + γ·V_{t+1} (γ=0.99) — proves the score
r̂ + ΔΦ̂ + γ·V̂_next neither reuses the current value nor double-counts
r_t; (3) terminal next-state value exactly zero. A1 (fixed p4_e2e_v1_wm
run per the 2026-07-28 spec) is next.

## A1 status: trainer built, smoke PASSED, p4_e2e_v1_wm RUNNING

`lcwm/p4_train.py` + `scripts/train_p4_e2e.py` per the M1/M2 fixed spec:
simulator-free (no chassis import, no 3B load), source-balanced schedule
(19 sequential sources/epoch, 5 chain sources × 1 rotating admitted group),
per-source family means then weight-1 combination, P2-v3 optimizer budget
(AdamW 3e-4/1e-4, EMA 0.996, TBPTT 16, 4-source VIC 25/1, locked scales,
Huber 4), dq/dobj outputs zero-inited, dprog/ret frozen, adapter frozen
with bitwise assertion, checkpoints every 5 epochs, strict-reload bundle.

Plumbing smoke PASSED: 1 sequential + 1 branch, 2 steps, all families
finite (self 0.947, residual 58.4, physical 11.9, next_bits 0.747, reward
0.042, dphi 0.203), strict reload reproduces outputs exactly, adapter
bitwise unchanged. Full run launched: seed 0, 30 epochs,
`results/lc_flow_v04_e2e_v1/world_model/`. No sweeps.

## A1 result: p4_e2e_v1_wm training COMPLETE (fixed budget, no sweeps)

30/30 epochs, seed 0, checkpoints every 5 epochs + strict-reload final
bundle (`results/lc_flow_v04_e2e_v1/world_model/`). Per-loss trajectory
(epoch 4 → 29): self 0.031→0.013, residual 63.7→10.2, physical 25.8→17.7,
next_bits 0.57→2.10 (noisy, small sample per epoch), reward 0.33→0.051,
ΔΦ 0.011→0.012, v_next 0.478→0.289. Adapter asserted bitwise unchanged.
Offline readout only — per the queue, no gate applied; proceeding to A2.

## A2 result: candidate_teacher.pt COMPLETE — value-driven positive support

207/207 actionable late-chain decisions (exactly the predicted count),
teacher diagnostics (`teacher_diagnostics.json`):

- positive-teacher fraction **73.4%**, uniform across sources (70–82%);
- advantage quantiles [0.0055, 0.0247, 0.0942] — small but consistently
  positive; teacher weights land at ≈1.01–1.10;
- **component decomposition inverts risk #3**: on positive teachers the
  mean advantage decomposes as r −0.0007, ΔΦ −0.0082, γ·V̂_next **+0.0540**
  — selection is driven ~entirely by the LEARNED continuation value, not
  the manual ΔΦ term;
- honest caveat: every 2/3-state V^π0 TRAINING target is 0 (no late-chain
  success exists in the data), so the V̂_next candidate differences are
  extrapolation by the value head from earlier states. Whether that
  extrapolation carries real signal is exactly what the A3 closed loop
  tests; per the queue these diagnostics set no gate.
- candidate diversity: mean first-ten L2 vs stock 0.125.

Spec deviation recorded: `paired_flow_noise_time` (referenced by M4) does
not exist in the codebase; the adapter uses deterministic per-item seeded
noise/time instead.

## A3 launched: model-guided adapter (M4 fast-pilot budget)

`scripts/train_model_guided_adapter.py`: wz-only training, teacher weight
1+clip(A,0,1) one-hot, first-ten credit via credit_bounded_actions,
full-50 stock rehearsal from the 143-decision early pool (1:1 mixture),
8 stratified late states per source per epoch with cyclic offset, 5 epochs
AdamW 1e-4, startup regression asserts suffix-gradient separation.

## A3 training result: adapter moved substantially; N=1 eval running

Suffix-credit regression PASSED at startup (rehearsal receives 10–49
gradient; model-guided first-ten gradient bitwise invariant to suffix).
5 epochs: teacher flow loss 0.172→0.047 (29–32 positive items/epoch of 40,
matching the 73.4% fraction), rehearsal 0.071→0.078, |wz_out| 0→1.010.

Fixed-state first-ten action shift vs stock (5 mid-late states, one per
source, shared noise): L2 = 0.139 / 0.222 / 0.894 / 0.231 / 0.474 on a
~0.48 action scale — **substantial, not the C1 negligible-shift mode**.
Sanity value only, not a gate.

Eval checkpoint assembled (`eval_adapter/`, schema lc_flow_adapter_v1:
WM LCState + trained wz, strict reload verified, |wz_out|=1.010). Paired
N=1 running: 2 arms × 5 seeds (1048/1058/1068/1078/1088), full Chain3
prompt, paired env seeds and flow noise, no checkpoint selection.

## A3/M5 RESULT: paired 5-seed N=1 — first closed-loop full-chain success

`results/lc_flow_v04_e2e_v1/paired_n1_5seed.json` (2 arms × 5 seeds, full
Chain3 prompt, paired env seeds + flow noise, no checkpoint selection):

| seed | stock Q (atoms@step) | v0.4-e2e-v1 Q (atoms@step) |
|---|---|---|
| 1048 | 0.667 (150, 270, —) | 0.667 (150, 260, —) |
| 1058 | 0.667 (130, 240, —) | 0.667 (130, 230, —) |
| 1068 | 0.667 (120, 230, —) | 0.667 (120, 240, —) |
| 1078 | 0.667 (150, 260, —) | **1.000 (140, 250, 670) SUCCESS** |
| 1088 | 0.667 (140, 260, —) | 0.667 (130, 260, —) |

Aggregate: SR **1/5 vs 0/5**, mean Q **0.733 vs 0.667**, no seed worse
than stock. Seed 1078 is the FIRST full-chain closed-loop completion in
this project's recorded history (stock completed cream cheese in 0/15 exam
episodes, 0 branch continuations, and 0 prior closed-loop runs).

### Analysis: what this is and is not

- **Existence proof, not a statistical claim.** One success in five paired
  seeds. The correct statement: the model-to-policy loop CAN produce a
  full-chain completion where stock never has; on 4/5 seeds it ties (with
  soup/sauce ~10 steps earlier on 3 of them).
- **Why it can work (mechanism trace):** teacher advantages were driven
  ~entirely by learned V̂_next extrapolation (A2 decomposition), the
  adapter absorbed those targets with substantial first-ten shift
  (0.14–0.89 L2), and on 1078 that shift carried the policy through
  grasp/place of cream cheese.
- **Why probably only 1/5:** (i) the teacher's V̂ is extrapolation — all
  207 training states carry V target 0, so guidance quality is uneven
  across states (user risk #1/#3 in weakened form); (ii) policy bandwidth:
  ~10.7k adapter params through one pooled AdaRMS bias (risk #2); (iii)
  support: 207 highly correlated decisions from 5 episodes.
- **Attribution is OPEN:** per the 07-29 interpretation table this win is
  not yet evidence for the world-model mechanism — B0 (belief-BC without
  model guidance) and B1 (no-recurrence control) must run on the identical
  protocol before any mechanism claim. LIBERO-10 retention has NOT been
  run; no promotion claim is made.

### Route recommendation (per the 07-29 tables)

1. **B0/B1 immediately** (attribution): same budget, same 5-seed protocol.
2. **C3-lite with a new lever:** the 1078 success rollout is itself the
   first successful late-chain trajectory — replaying/labeling it yields
   the first NONZERO late-chain V^π0 targets, directly attacking the
   weakest link (value extrapolation without success support). Recommend
   folding it (and 16–32 targeted late-chain sources per C3) into a P4
   retrain before N=8 candidates.
3. C1 (PEFT) is NOT triggered: action shift is substantial.
4. Retention slice before any promotion of v0.4-e2e-v1.

Phase I artifacts (all four exist): trained `p4_e2e_v1_wm`,
`candidate_teacher.pt`, adapter checkpoint, `paired_n1_5seed.json`.

---

# Active supersession — matched attribution and long-horizon performance

## First-principle interpretation

Phase I is a real model-to-policy existence result:

```text
trained WM
    → selected policy targets
    → changed deployed policy
    → one full Chain3 completion
```

It is not yet evidence that learned dynamics caused the improvement. Three
facts set the next action items:

1. with three fresh π0.5 candidates and one exchangeable stock sample, the
   null probability that a fresh candidate scores above stock is approximately
   \(3/4=75\%\); the observed positive-teacher fraction is `73.4%`, so that
   fraction alone is not evidence of useful ranking;
2. selected advantages are driven by \(\hat V_{\mathrm{next}}\), while every
   late-chain \(V^{\pi_0}\) training target is zero; the score is therefore an
   off-support extrapolation that must be tested through a matched policy
   control;
3. the scalar `1 + clip(A, 0, 1)` weight was normalized by its own
   `weight_sum` for batch size one. Phase I is accurately described as
   **WM-selected, uniformly weighted teacher finetuning**, not
   advantage-weighted finetuning.

The earlier recommendation to fold seed 1078 into \(V^{\pi_0}\) training is
withdrawn. That trajectory was generated by `v0.4-e2e-v1`, so its return has
policy provenance \(\pi_{\mathrm{LC}}\), not \(\pi_0\). It is also a development
evaluation trajectory. If it is ever used for training, seed 1078 and the
current five-seed panel must be retired from subsequent evaluation and replaced
by a newly registered held-out panel.

The active objective is now:

```text
Does WM-selected policy training beat a matched score-blind selection control?
        ↓
Does that difference survive 700/900/990-step full-prompt N=1 tasks?
```

The previous immediate `B0/B1 → C3-lite` route is superseded by the queue below.
No new data or architecture is placed in front of this comparison.

---

## H0 — Freeze and name the Phase-I artifact honestly

- [ ] Preserve the existing world model, teacher, adapter, and five-seed JSON
  bit-for-bit as `v0.4-e2e-v1-wm-selected-uniform`.
- [ ] Add one reproducible assembly script that constructs `eval_adapter/`
  from the hashed WM checkpoint and adapter weights; do not rely on an
  unrecorded one-off merge.
- [ ] Record hashes for code, source manifests, WM checkpoint, candidate
  teacher, policy adapter, and evaluation adapter in one run manifest.
- [x] Correct the corrupted text encoding in this file without changing any
  numeric result.
- [ ] Preserve the original five-seed JSON unchanged; put corrected per-seed
  `known_limits` in a hash-linked sidecar instead of violating the bit-for-bit
  freeze.
- [ ] Replace the over-strong word `equivalent` in `lcwm/loho.py` with
  `LoHo-inspired`; reserve `LIBERO-LoHo` for a future official integration.
- [ ] Do not retroactively claim advantage weighting, statistical
  significance, LIBERO-10 retention, or language-conditioning evidence.

**Exit:** the Phase-I result is reproducible from one manifest and retains its
observed boundary: Chain3 development SR `1/5` versus `0/5`.

---

## H1 — Evaluate the existing policy on genuinely longer chains now

Do not wait for another training run before obtaining the first longer-horizon
readout.

### Tasks

| task | horizon | atoms | role |
|---|---:|---:|---|
| `chain3_lr2` | 700 | 3 | existing development anchor |
| `chain4_lr2` | 900 | 4 | first longer-horizon transfer |
| `chain5_lr2` | 990 | 5 | longest executable chain under the simulator cap |

These are real closed-loop 700–990 step tests on the repository's self-built
LoHo-inspired suite. They are **not** the official LIBERO-LoHo benchmark; no
official LoHo integration or dataset is currently present in this repository.

### Observed Chain3 anchor and fresh longer-chain panel

The existing Chain3 seeds `1048, 1058, 1068, 1078, 1088` are now an observed
development panel. Preserve their stock/full results, but do not relabel them
as a new held-out test and do not use seed 1078 as \(\pi_0\) training data.

For the first complete longer-chain readout:

- [ ] Register fresh environment seeds
  `1098, 1108, 1118, 1128, 1138` before launching evaluation.
- [ ] Immediately run stock π0.5 and the frozen Phase-I adapter on both
  `chain4_lr2` and `chain5_lr2`: two arms × two tasks × five seeds =
  **20 real long-horizon episodes**. Chain5 is no longer gated by Chain4.
- [ ] In parallel, train H2 and H3. When they finish, evaluate all four arms
  plus stock on Chain3/4/5 with the same fresh seeds, producing a balanced
  **5 arms × 3 tasks × 5 seeds = 75-episode development matrix**.
- [ ] Do not omit a task/arm cell because an earlier cell failed. The purpose
  of the complete matrix is to distinguish failure to compose, failure to
  maintain earlier atoms, and failure of the proposed WM mechanism.
- [ ] Use a different deterministic flow-noise stream for every
  `(task, environment_seed)`, shared exactly across arms:

  ```text
  noise_seed(task, env_seed, decision)
      = 20_000_000
        + task_index * 2_000_000
        + env_seed * 1_000
        + decision
  ```

  with task indices Chain3/4/5 = 0/1/2.
- [ ] Keep the original Phase-I Chain3 result intact and label it
  `noise_contract=legacy_chain3_v1`: it reused policy-noise base `9_028_000`
  across environment seeds. Do not append new arms to it or mix it into the
  new statistics; it does not replace the fresh-seed Chain3 row in the
  balanced matrix.
- [ ] Report only `prompt_mode=full` composite-prompt rollouts as learned
  long-horizon behavior. The existing `decomp` mode reads oracle predicates
  and switches atomic instructions, so it is an oracle diagnostic rather than
  a policy result.

Chain4 is the goal union of two standard LIBERO-10
`LIVING_ROOM_SCENE2` tasks, while Chain5 adds an atomic skill represented in
LIBERO-90. This makes the suite a useful known-scene compositional exam, but
not an OOD benchmark. The Phase-I claim sought here is improved closed-loop
composition over longer horizons, not unseen-scene generalization.

Here `N=1` means one 50-action flow sample at each decision; the environment
executes its first ten actions and replans. Sharing the decision-indexed noise
stream is common-random-number variance reduction after policies diverge, not
a matched-state intervention.

### Metrics

Report per seed and per task:

- [ ] all-goal success and success time;
- [ ] final Q;
- [ ] final ordered-prefix depth
  \(D_{\mathrm{final}}=\max\{k:b_1,\ldots,b_k=1\}\);
- [ ] maximum ordered-prefix depth reached at any time;
- [ ] normalized ordered-prefix area under the rollout curve
  \(A_D=(TK)^{-1}\sum_{t=1}^{T}D_t\), where \(K\) is the number of atoms;
- [ ] per-atom first-completion time and whether the atom is later damaged;
- [ ] first unresolved atom, failure phase, and episode steps;
- [ ] paired deltas for every arm.

Q alone is insufficient on Chain4/5 because a later object may be completed
while an earlier atom remains unresolved.

### Evaluator artifact

Add `scripts/eval_long_horizon_paired.py` by reusing `LCFlowRunner`,
`make_chain_env`, and `run_chain_episode`.

- [ ] Accept an arbitrary list of named adapter arms, tasks, and seeds.
- [ ] Implement stock as the same seeded LC sampler with `wz_out == 0`, and
  assert exact stock-policy parity before evaluation. The native sampler does
  not expose the same flow-noise contract.
- [ ] Read an immutable manifest and append one authoritative record keyed by
  `(run_id, task, seed, arm)`; `--resume` must verify the manifest hash and
  abort on duplicate/conflicting completed records.
- [ ] Save code/environment, policy/config/BDDL hashes and the complete noise
  schedule. Interleave arms within each `(task, seed)` block using one
  pre-registered hash permutation.
- [ ] Remove the old hard-coded one-seed/v3 `known_limits`.
- [ ] Do not extend `scripts/run_chains.py`: it truncates its JSONL at startup.
- [ ] Save normalized and environment action traces for every episode. Save
  decision-boundary q/object traces if `failure_phase` is reported; otherwise
  report only the first unresolved atom.
- [ ] Save original success videos; deterministic replay may validate them but
  can never replace the authoritative rollout result.

Maintain an explicit run ledger with one row per `(task, seed, arm)`, including
`pending/running/complete/failed`, retry count, wall time, checkpoint hashes,
and output paths. A crashed episode may be resumed, but a completed episode is
never silently overwritten.

**Artifact root:**

```text
results/behavior_factorial_v1/
  run_manifest.json
  development/{records.jsonl,summary.json}
  confirmation/{records.jsonl,summary.json}
  retention/{records.jsonl,summary.json}
  language_robustness/{records.jsonl,summary.json}
  traces/
  videos/
```

---

## H2 — Train the sharp matched score-control

Build `v0.4-e2e-v1-random-selected-uniform`. It must differ from the existing
full arm in exactly one place: candidate selection.

### Locked matching contract

- [ ] Reuse the exact trained WM recurrent states, source/state schedule,
  zero-initialized policy adapter, optimizer, five epochs, 143-state rehearsal
  pool, flow noise/time, and `1:1` rehearsal mixture.
- [ ] The current teacher artifact does not retain all three non-stock chunks.
  Regenerate each deterministic `N=4` candidate pool from its saved
  `candidate_seed`, then assert that the regenerated stock and WM-selected
  chunks reproduce the recorded hashes before constructing the control.
- [ ] Reuse the same 152 positive-state mask on which the WM arm selected a
  non-stock teacher.
- [ ] At each positive state, choose one of candidates 1–3 from a fixed hash of
  `(source_id, decision_index, control_seed=0)`; do not inspect model scores.
- [ ] Keep teacher losses uniformly weighted to match the Phase-I behavior.
  Do not fix scalar advantage weighting in only one arm.
- [ ] Report selected first-ten action distance/diversity so an accidental
  action-magnitude mismatch is visible.
- [ ] Assert that every non-selection input, optimizer update count, and
  rehearsal item matches the full arm.

**Artifact:**

```text
results/lc_flow_v04_e2e_v1_random_control/
  teacher_manifest.pt
  policy_adapter/
  action_shift.json
```

This is a policy finetuning experiment, not an offline ranking diagnostic.
Because it reuses the 152-state WM-positive mask, its precise interpretation is
score-blind candidate choice on the states admitted by the WM arm—not a
completely WM-free data-selection baseline.

---

## H3 — Complete the selection × recurrence factorial

Build two current-only arms:

- `v0.4-e2e-v1-current-only-wm-selected`;
- `v0.4-e2e-v1-current-only-random-selected`.

For both arms:

- [ ] reuse the corresponding H0/H2 candidate chunks and the exact same
  152-state positive mask; the current-only random arm must reuse H2's random
  choice rather than draw another candidate;
- [ ] replace the recurrent history state with
  \(z_t^{\mathrm{current}}=U(z_0,h_t)\) independently at every decision;
- [ ] preserve state dimension, `wz_hidden/wz_out`, initialization, optimizer,
  rehearsal, update count, and training/evaluation seeds;
- [ ] deploy with the same current-observation-only state rule.

These arms isolate whether persistent visual-action history contributes beyond
the current observation while keeping the policy targets fixed. The second
current-only arm completes the factorial and makes the selection × recurrence
interaction identifiable. Both H3 jobs train in parallel with H2.

The primary attribution panel is:

| arm | state supplied to policy | teacher selection |
|---|---|---|
| stock | none | none |
| `rec_random` | recurrent WM state | fixed random non-stock |
| `cur_random` | current observation state | same fixed random non-stock |
| `cur_wm` | current observation state | WM-selected |
| `rec_wm` / full | recurrent WM state | WM-selected |

Interpretation:

- `rec_wm > rec_random`: learned selection contributes under recurrence;
- `cur_wm > cur_random`: learned selection contributes without recurrence;
- `rec_wm > cur_wm`: recurrent history contributes under the WM teacher;
- `rec_random > cur_random`: recurrence contributes under the random teacher;
- \((\text{rec_wm}-\text{rec_random})
  -(\text{cur_wm}-\text{cur_random})\) is the interaction;
- `rec_random ≈ rec_wm > stock`: generic supported-action post-training or the
  recurrent interface, not learned scoring, explains the gain;
- all trained arms ≈ stock: the original 1/5 result was not stable.

This factorial isolates what the deployed policy consumes and how its teacher
chunk was chosen. It does not yet isolate predictive-loss training from every
possible matched-capacity state learner; if the full arm wins, a no-world-loss
state control remains required for a final LCWM mechanism claim, but it does
not precede the behavior experiment.

---

## H4 — Run the matched long-horizon panel

Complete every cell in the registered development matrix:

| arm | Chain3 × 5 | Chain4 × 5 | Chain5 × 5 | total |
|---|---:|---:|---:|---:|
| stock π0.5 | 5 | 5 | 5 | 15 |
| `rec_random` | 5 | 5 | 5 | 15 |
| `cur_random` | 5 | 5 | 5 | 15 |
| `cur_wm` | 5 | 5 | 5 | 15 |
| `rec_wm` / full | 5 | 5 | 5 | 15 |
| **total** | **25** | **25** | **25** | **75** |

The first tranche is the 20 stock/full Chain4/5 episodes from H1, while H2/H3
train. The second tranche adds the other three arms and the fresh Chain3 row.
There is no behavior-dependent Chain5 gate.

```text
long-horizon performance                 mechanism attribution
------------------------                 ---------------------
stock/full on Chain4 + Chain5            H2 random-teacher finetuning
                 │                       H3 two current-only finetuning jobs
                 └──────────┬────────────┘
                            ↓
                five arms × Chain3/4/5 × five seeds
```

### Pre-registered comparisons

1. **Performance:** `rec_wm − stock`.
2. **Selection under recurrence:** `rec_wm − rec_random`.
3. **Selection without recurrence:** `cur_wm − cur_random`.
4. **History under WM selection:** `rec_wm − cur_wm`.
5. **History under random selection:** `rec_random − cur_random`.
6. **Selection × recurrence interaction:** difference of the two selection
   contrasts.
7. **Generic post-training effect:** `rec_random − stock`.

All-goal SR on Chain4 and Chain5 is the primary behavioral result. Because SR
may be sparse, normalized ordered-prefix AUC is the primary failure-localizing
result; final Q, maximum prefix, and per-atom timing are secondary. Report each
chain separately before showing a macro average. Never use a pooled number to
hide a Chain5 regression.

---

## H5 — Confirmation, retention, and data refresh

### Development interpretation

The 75-episode matrix is development evidence, not a significance claim.
Separate two questions:

- **Performance trigger:** full improves over stock on Chain4/5 all-goal SR or
  normalized ordered-prefix AUC without a material regression on either task.
- **Mechanism trigger:** `rec_wm` also improves over `rec_random` and `cur_wm`
  in the comparisons each control was designed to isolate; the remaining
  factorial contrasts localize any interaction.

A performance trigger without a mechanism trigger supports a useful
post-training result, but not the intended LCWM attribution.

### Sealed confirmation

When the performance trigger is reproducible:

- [ ] freeze all checkpoints, prompts, evaluator code, metrics, and analysis;
- [ ] run the five registered arms on Chain3/4/5 using new environment seeds
  `1208, 1218, 1228, 1238, 1248, 1258, 1268, 1278, 1288, 1298`;
- [ ] complete the full **5 × 3 × 10 = 150-episode** matrix without adaptive
  stopping;
- [ ] retain the per-`(task,seed)` noise formula from H1;
- [ ] report exact counts and paired deltas, plus 95% seed-clustered bootstrap
  intervals for SR and normalized prefix AUC;
- [ ] keep task-level results visible and treat environment seed, not decision
  step, as the independent unit.
- [ ] for each seed, also report atom-weighted final prefix
  \(S_{\mathrm{prefix}}=(D_3+D_4+D_5)/12\); use a fixed 10,000-resample
  cluster bootstrap and an exact sign-flip test for the three primary
  contrasts `rec_wm−stock`, `rec_wm−rec_random`, and `rec_wm−cur_wm`, with
  Holm correction.

This confirmation supports a fixed-policy, policy-seed-0 conclusion. After it
is complete, reproduce the four trained arms with adapter optimizer seeds 1
and 2 using the locked contract and report Chain4/5 development mean/range
without selecting the best seed. Multi-world-model-seed stability is a later
paper-level requirement, not a gate placed before the first complete behavior
result.

### Standard-task retention

Before promotion, run all five arms on all ten LIBERO-10 tasks using fixed
initial-state indices `0, 10, 20, 30, 40`:
**5 arms × 10 tasks × 5 states = 250 episodes**. Stock must be rerun through
the same evaluator. Report per-task and macro SR plus steps to success;
promotion requires `rec_wm` to regress by no more than five absolute macro-SR
percentage points relative to stock and not collapse systematically on any
single task. This is a retention guard, not a powered non-inferiority claim.

### Language robustness

After the canonical-prompt matrix, register two semantically equivalent
paraphrases for each of Chain4 and Chain5. Run stock and full on the five
development seeds with separate prompt-indexed noise streams:
**2 tasks × 2 paraphrases × 2 arms × 5 seeds = 40 episodes**. Report behavior
relative to each arm's canonical prompt. This is a task-identity robustness
test; it does not by itself prove latent equivalence.

### Failure routing

- `rec_wm ≈ rec_random` and `cur_wm ≈ cur_random` → selection did not add
  value; do not collect more value data before checking generic supported-
  action post-training;
- `rec_wm > rec_random` but `rec_wm ≈ cur_wm` → selection helps, persistent
  history has not yet earned an attribution claim;
- `rec_wm` beats its matched controls but fails Chain4/5 → collect task/action
  support for the first unresolved later atom, then retrain the same
  architecture;
- all arms move actions but fail equally → improve policy supervision before
  increasing adapter capacity;
- full has reliable score advantage but insufficient action influence → only
  then enter action-expert PEFT.

### Data provenance

- [ ] Keep the seed-1078 success as a frozen
  `generating_policy=v0.4-e2e-v1` development artifact.
- [ ] Never label its return as \(V^{\pi_0}\).
- [ ] If it enters a future training set, retire the complete
  1048–1088 development panel and use only the newly registered held-out
  confirmation seeds for evaluation.
- [ ] Any new 16–32 late-chain sources record their generating policy and split
  by independent source episode before collection begins.

---

## Immediate execution order

```text
H0 freeze/provenance correction
        ├── H1 stock/full on fresh-seed Chain4 + Chain5 now
        ├── H2 matched random-teacher finetuning
        └── H3 two matched current-only finetuning jobs
                         ↓
        H4 complete five-arm Chain3/4/5 development matrix
                         ↓
        H5 sealed 150-episode confirmation + LIBERO-10 retention
                         ↓
        adapter-seed stability + prompt robustness
```

If compute is temporarily constrained, preserve this priority:

1. stock/full Chain4 and Chain5;
2. random-recurrent and both current-only training jobs;
3. complete the 75-episode canonical matrix;
4. sealed confirmation;
5. retention, adapter-seed stability, and paraphrases.

The next status update should contain actual five-seed Chain4 **and Chain5**
`N=1` numbers plus training status for all three new factorial arms. It should
not contain a new WM architecture, a larger candidate set, or seed-1078
training before those artifacts exist.

The registered fixed-policy behavior budget is 75 development + 150
confirmation + 250 retention + 40 paraphrase episodes = **515 episodes**.
Only the 75-episode development matrix is unconditional; confirmation and the
remaining panels open after the registered performance trigger.

---

# Superseded historical continuation — do not execute

The entries below predate the active supersession. In particular, their
recommendation to relabel seed 1078 as \(V^{\pi_0}\) or proceed directly to
`C3-lite` is withdrawn and retained only as execution history.

## B0 attribution result: belief-BC 0/5 — first controlled evidence FOR
## the model-guidance mechanism

B0 (`--mode belief_bc`): identical recurrence, capacity, optimizer budget,
stratified schedule, rehearsal, and even final adapter norm
(|wz_out| 1.021 vs 1.010) — the ONLY difference is stock-chunk targets in
place of model-selected candidates. Result: **0/5, all ties at Q=0.667,
including seed 1078** (`paired_n1_b0_5seed.json`).

Per the registered interpretation table: the model-guided arm succeeded
where the matched belief-BC arm did not → "learned predictive scoring
provides additional policy improvement." Scope: N=5, one success seed —
controlled but small; B1 (no-recurrence) is now lower priority because B0
already shows recurrent conditioning alone does not explain the gain.
Caveats standing: no retention slice yet, no promotion claim, teacher's V̂
support remains the weakest link.

### Night summary (Phase I + B0)

stock 0/5 · v0.4-e2e-v1 **1/5** (first-ever full-chain closed-loop
success) · belief-BC 0/5. Recommended next: C3-lite — relabel the 1078
success rollout into the first nonzero late-chain V^π0 targets + 16–32
targeted late-chain sources → P4 retrain → re-run the same protocol;
retention slice before any promotion.

---

# Execution log (Claude) — H-queue

## H0 DONE (9e4b0d3)

Frozen as `v0.4-e2e-v1-wm-selected-uniform`: run_manifest.json with all
component hashes (WM/teacher/adapter/eval-adapter/five-seed JSON/code
commit), claim boundary and legacy noise contract recorded;
`scripts/assemble_eval_adapter.py` reproduces eval_adapter BIT-IDENTICALLY
(sha 949f76f0… verified); known_limits sidecar hash-linked to the frozen
JSON (untouched); loho.py wording → "LoHo-inspired".

## H1 harness built + 20-episode longer-chain panel RUNNING

`scripts/eval_long_horizon_paired.py` (behavior_factorial_v1): fresh noise
contract 20e6+task_index·2e6+env_seed·1e3+decision shared across arms;
stock = frozen adapter with wz_out zeroed in-loop (structural assert; the
bitwise zero-LC==stock parity stands from the wiring tests);
prompt_mode=full only; immutable manifest + (run_id,task,seed,arm) records
+ resume-with-hash-verify + no-overwrite ledger; hash-permutation arm
interleaving; env-action traces + noise schedules per episode (normalized
recoverable via exact stored inverse — recorded choice); metrics incl.
D_final/D_max/A_D/per-atom first-completion+damage/first-unresolved.
Launched: run_id h1_longer_chains_v1, arms {stock, v04e2e}, chain4+chain5,
fresh seeds 1098–1138 (registered), 20 episodes. Video capture deferred —
recorded as an open harness item, not silently dropped.

Next: read H2/H3 specs and launch the three factorial training arms while
the panel runs.
