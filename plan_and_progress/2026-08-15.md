# 2026-08-15 — mainline action item 1 EXECUTED: evidence sealed by content

Registration for the current mainline lives in `2026-08-09.md` ("Next mainline
action items", 7 items) and `framework_design.md` v0.9 §11. This daily is the
execution log for those items. Item 1 is complete; items 2–7 are not started.

## Item 1 — "Land and freeze the evidence before new execution"

Registered text:

> Commit the actual V7.5 architecture, trainer, gate, daily amendment, and
> lightweight manifests; bind source-file hashes directly because the recorded
> Git SHA does not contain them. Preserve every new immutable input index
> before any cache expansion. Do not delete source/shard tensors required to
> unroll a claimed training universe.

Four separable deliverables, all executed.

### 1a. Content seal — `scripts/seal_v075_provenance.py`

Output: `2026-08-15_v075_seal_r1/provenance_seal.json`
(schema `v075_provenance_seal_v1`, 38 KB).

The seal binds V7.5 by sha256 of file **content**, because the run's own
lineage pointer is wrong and committing the code now cannot retroactively
correct a manifest written before the commit existed. Committing alone would
have produced a repository where the code is present and every manifest still
points at the wrong commit; the seal is what makes the two reconcilable.

Four parts:

- **code** — 25 files. The set is a static import closure from the six V7.5
  entry points (`build_v075_hcache`, `train_v075_lcwm`, `gate_v075_binding`,
  `diag_v075_recurrence`, `diag_v075_repair_check`, `test_v075_contract`) over
  repo-local modules, *derived rather than hand-listed*, so an inherited
  dependency (`v06_model`, `v073_schedule`, `v072_schedule`, …) cannot silently
  drop out of the record. The seal hashes itself too.
- **artifacts** — 19 files: all six checkpoints, both gate tensor files, every
  metric/manifest JSON, the surviving h-cache index. The 5825 h-cache tensors
  (23 GB) are excluded by policy and the index that names them is hashed
  instead.
- **inputs** — the seven v074/semwin lightweight files the run read.
- **universe_integrity** — all 33 acq anchors (24 train + 9 dev) verified
  loadable, and their 30 source + 33 shard tensors (5.7 GB) hashed. Existence
  was already asserted by `load_universe()`; identity was not, so a silent
  replacement was previously undetectable. It is now.

### 1b. Cross-checks — 9 run, all behaved as recorded

Checks are typed. `must_hold` failures exit nonzero; `known_broken` checks
assert a defect is **still** broken, so a later silent "fix" cannot pass
unnoticed.

| check | status |
|---|---|
| `gate.lcwm_sha256` == sha256(`checkpoints/final.pt`) | match |
| md5(`final.pt`) == md5(`lcwm_epoch024.pt`) | match |
| `gate.anchor_states.sha256` == sha256(on disk) | match |
| `gate.centering.sha256` == sha256(on disk) | match |
| `run_manifest.input_sha256.v074b_anchor_manifest.json` | match |
| `hcache_manifest.input_sha256.v074_anchor_manifest.json` | match |
| `hcache_manifest.input_sha256.text_variants.json` | match |
| `run_manifest.input_sha256.hcache_index.json` | **mismatch (expected)** |
| `run_manifest.init.state_sha256`, **recomputed** | match |

Two of these are worth naming.

**The fresh initialization reproduced.** `torch.manual_seed(0)` then
`V075State()` on CPU regenerates state sha `2a1734430f281736…` exactly. The
daily's "fresh seed-0 init" claim is now independently verified from the
committed source rather than asserted from the trainer's own log — the first
end-to-end reproduction of any V7.5 quantity outside the run that produced it.

**The h-cache index mismatch is confirmed, not repaired.** Recorded
`ef9d4b15…`, actual `51bf1b9a…`. The 3978-key training index (2394 unroll +
1584 next-obs) is gone. It also cannot be reconstructed by filtering the
survivor: `5825 − 1685 demo = 4140 ≠ 3978`, so the training key set is not a
recoverable subset of the present one. Recorded as permanent, per the 8/14
audit.

### 1c. The overwrite cannot recur — `preserve_and_write`

`scripts/build_v075_hcache.py` wrote `hcache_index.json` and
`hcache_manifest.json` with a bare `write_text`, so the second invocation
(`--stage policy`) destroyed the first (`--stage lcwm`). That single line is
the whole cause of the lineage loss above.

Replaced with `preserve_and_write(path, payload, stage)`, which treats an index
as an **immutable input** once a run has trained against it:

1. identical bytes → no-op;
2. differing prior version → copied to
   `index_archive/<stem>__<sha12>.json` before anything moves;
3. new version → also frozen at
   `index_archive/<stem>__<stage>__<sha12>.json`; an existing
   content-addressed name is asserted byte-equal, never rewritten;
4. only then does the convenience pointer move, and a row lands in the
   append-only `index_lineage.jsonl`.

`.gitignore` gains `results/**/index_archive/` — the preserved copies live on
disk (they are 1.1 MB of absolute paths each, matching the existing
`hcache_index.json` exclusion), while the compact `index_lineage.jsonl` audit
trail is what gets committed.

### 1d. Recorded fidelity loss, self-inflicted, named not hidden

The builder patch in 1c was applied **before** the seal ran, and the builder
was untracked, so its run-exact bytes are unrecoverable. The seal therefore
marks `scripts/build_v075_hcache.py` with `run_exact: false` and states that
its hash binds the current repository only. The other 24 code files are
`run_exact: true`.

This is a small real loss and it is recorded rather than papered over. What the
builder produced remains bound independently by `hcache_manifest.json`
(enumeration rule, counts, input hashes) and by the surviving index hash.

### Status after item 1

`git_sha 717a9da` is preserved verbatim in every V7.5 manifest and is still
wrong; it is now *reconcilable*, because the seal names the exact content those
manifests should have pointed at and that content is committed. Any future
claim about V7.5 can be checked by re-running the seal.

Items 2–7 (data tranche → action-identifying LCWM → interface check → ledgers →
π0.5 fine-tune → behavior) are unchanged and unstarted.
