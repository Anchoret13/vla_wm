# Progress — 2026-07-20

Response to the independent Codex audit (recorded at the end of `2026-07-19.md`)
and the follow-on confound work it triggered. Commits `b8a28a8` → `db442fe` →
`f426378`.

## Audit response — every factual claim verified TRUE and addressed

1. **Orientation (audit #4) — CONFIRMED EMPIRICALLY** by a quadrant-blackout test
   (`scripts/orientation_check.py`): blacking the obs top-left changes token
   bottom-right (|Δ| 61.5 vs 17.8–29.0). Token grid is RAW-oriented;
   LiberoProcessorStep flips the already-flipped obs back (`env_processor.py:45`).
   `lcwm/seg_masks.py` projection de-flipped; all earlier heatmap overlays were
   180° misaligned in display (battery statistics unaffected — no spatial pairing).
2. **Selectivity rerun with corrected masks** — t=0 selectivity SURVIVES AND
   STRENGTHENS: distractors **0.013** vs t1_targets **0.461**, basket **0.743**,
   t0_targets 0.309 (~30×). Task swap most strongly re-codes the newly-relevant
   objects and the goal location. δ(const) shows the same shape at half magnitude.
   t=160 remains muddy (moved/occluded objects) — "precisely" holds at t=0 only.
3. **Probe bookkeeping (audit #8)**: 5,258 frames confirmed; 3 shards with
   collection-replay success=False (task2_demo7, task5_demo1, task8_demo11)
   EXCLUDED by documented rule; numeric demo sorting fixed (glob was
   lexicographic); objects per task 2–8; metadata (split ids, λ grid, seeds,
   exclusions, moving-dim rule) emitted in `results/probe_ladder.json`.
4. **World-probe metric v5→v6**: train-defined moving dims first exploded per-dim
   R² (train-moving dims with ~zero test variance → −3000s); final metric =
   pooled variance-weighted R² + mean position error (cm), 3 seeds:
   siglip 0.698 [3.2cm] demo-split / 0.851 [2.2cm] frame-split (const 0.666/0.824,
   real 0.643/0.833). **Superseded by the retraction below.**
5. **True task-only prior added** (audit #6): one-hot task-ID gives phase R²
   **0.000** / remaining 0.070 — visual streams' 0.82–0.88 progress signal is
   genuine; e_lang phase drops to 0.616 under the corrected split (post-fusion
   pooled text ≠ language prior, confirmed).
6. Tap wording downgraded to **PROVISIONAL v0 DEFAULT** per the audit.

## PRE-REGISTERED: world-probe confound controls → v6 claim RETRACTED

Suspicion (raised in review discussion): v6's 0.698 is dominated by
manipulated-object dims that co-move with the gripper — arm tracking, not object
perception. Design: A token probe all frames | B q-only ridge | C token probe on
pre-contact test frames | D q-only pre-contact. Pass rule fixed in advance:
C > 0.2 AND C − D > 0.2.

| stream | A all-frames | B q-only | C pre-contact | D q-only-pre |
|---|---|---|---|---|
| siglip | 0.698 [3.2cm] | 0.641 [3.9cm] | −2.09 [2.5cm] | −5.85 [3.7cm] |
| const_ll | 0.666 | 0.641 | −3.38 [2.8cm] | −5.85 |
| real_ll | 0.643 | 0.641 | −3.60 [2.9cm] | −5.85 |

**A ≈ B: the v6 number was ~90% arm information (real_ll margin +0.002) —
RETRACTED.** C fails for all streams at 7 training layouts; genuine residual
signal (pre-contact absolute error 2.5 vs 3.7 cm) noted. Tasks 1/3/4 had only 3
pre-contact test frames.

## PRE-REGISTERED: layout probe set → ALL STREAMS FAIL

Properly-powered version: 50 init states/task, static settled frames, init-state
split 40 train / 10 test. Pass rule: R² > 0.3 AND margin over q-only > 0.3.

Result (`results/layout_probe.json`): siglip mean R² **0.035** (best; positive on
5/10 tasks, max 0.27; margin 0.19) | const_ll −0.378 | real_ll −0.314 |
q_only −0.158.

Interpretation boundaries: init-randomization spread ≈ 2 cm demands sub-cm
readout; siglip's absolute error (1.3–1.8 cm) consistently beats q-only
(1.7–2.4 cm) — the signal exists at the extraction edge; π0.5 itself acts at 96%
off these features. Verdict per rule: **decision-time cm-level layout reading via
frozen features + light probes is NOT certified.**

## Consequences (recorded in framework_design.md §3 evidence log)

- Implicit layout learning via WM dynamics on branch-scale data remains the
  primary route (A/B state encoders are a different regime from LocProbe).
- **Candidate-D's training-only auxiliary pose supervision promoted to a strong
  v0 auxiliary-loss candidate for A/B.**
- Task-side tap evidence (task-ID 1.000, role-selective modulation ~30×,
  paraphrase invariance) is the part of the tap story that stands firm.
- Branch probes remain the decisive test of what state the reranker needs.

## Process change (user-enforced, permanent)

Every probe/experiment: PRE-REGISTER question + trivial baselines (q-only,
task-prior, test-mean) + numeric pass/fail in the dated md BEFORE running.
Negative results are findings, never explained away. Root cause acknowledged:
prior iterations optimized metrics until numbers looked good instead of first
fixing what question the number must answer.

## Still open / next

- §13.2 formal N=16 diversity + end-state dispersion (pre-register first).
- Mask spatial-specificity check (disjoint, object-size-aware masks) — flagged
  because the pre-fix 180°-misaligned masks ALSO produced a "selective" pattern.
- Shard augmentation: `first_success_t` / terminal time / BDDL predicate bits.
- Branch schema freeze → C/A/B implementation → offline selection table.
