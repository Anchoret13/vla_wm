# 2026-08-29 — RECONSTRUCTED FROM COMMITS, not a contemporaneous log

**This file was written on 2026-09-16, not on 2026-08-29.** No daily log was kept that
day. A 2026-09-16 audit found 31 commits dated 2026-08-29 and no file to index them,
including the day that wrote framework v2 and reverted section 21 out of the binding
document — the day behind two still-open decisions. What follows is reconstructed from
`git log`, `git show` and the artifacts those commits touched.

**What a commit log cannot recover is listed at the end of this file.** Treat every
statement here as "the repository shows this", never as "this was decided for this
reason". Where the two are confused, the reconstruction is wrong and the commits are
right.

> **RECONSTRUCTED FROM COMMITS AND ARTIFACTS ON 2026-09-16. This is not a
> contemporaneous log.** Every statement below is traceable to a commit message,
> a `git show --stat`, or a file in `results/`. Reasoning, alternatives
> considered, and instructions received that day are **not** recoverable — see
> "What this cannot recover" at the end.

### Why the file is missing

Two reasons, both verifiable:

1. **The session ran past midnight and kept appending to the previous day's
   file.** `plan_and_progress/2026-08-28.md` was committed to four more times
   *on 2026-08-29* — `ac0e51cb` (01:39), `982fa6a3` (03:48), `a2ab4460` (15:47),
   `1c488734` (17:29) — adding sections that are, by content, 08-29 work:
   "SCOPE CORRECTION", "Limitations not previously stated", "Accumulated evidence
   on the transition's contribution", "v157/v163 — policy improvement in
   imagination" (lines 444, 487, 541, 593 of the archived file).
2. **The late-evening block was never written up at all.** Grepping
   `2026-08-28.md` for `Spearman | v167 | framework v2 | PROPOSED` returns zero
   hits, and `2026-08-30.md` opens straight into "executing framework v2" with no
   back-reference. So commits `6a3643f8` (22:39) through `8ac3cff0` (23:56) —
   the framework rewrite and the Spearman bug — have **no daily narrative
   anywhere**.

### Timezone note (matters for reading the record)

Commit dates are local `-0500` (CDT); result directory names are UTC. So
`results/…/2026-08-29T032135Z` is 2026-08-28 22:21 local, and
`results/v167_value_stratification/2026-08-30T045417Z` is 2026-08-29 23:54 local.
Below I use **local** times, to match the commit log.

### The day: 31 commits, 00:03 → 23:56 local

**00:03-00:05 — a withdrawal.** `06cb9744` "Add the rollout ablation, and fix
cp95 which was a 90% interval": an adversarial audit found
`train_v137_rank_head.py:44` gave the `direct` arm a *random-init* ActionEncoder
while the `wm` arm used T_θ's *pretrained* one, so v139 controlled for having a
pretrained action embedding, not for rolling the latent forward. The audit
reproduced `pre_enc` at 0.6600 against `wm` 0.6645. The claim "rolling the latent
forward ranks better than feeding the raw action" was **withdrawn**. Same commit
records that `clopper_pearson_{lower,upper}` defaulted to alpha = 0.05 *per side*,
so every field named `cp95` written before that day is a **90%** interval.
`3574181d` then trained the `pre_enc` head
(`results/v144_pre_enc_head/2026-08-29T050441Z/`).

**00:05-00:23 — the isolating deployments.**
`results/v141_branch_deploy/chain1b_lr2_wm_n32_s3.0_pre_enc_2026-08-29T050535Z`
(31,206 env steps) and `…_displacement_2026-08-29T052333Z` (31,253).

**01:29-01:57 — the rebuild.** `5c3a3c6e` `pi_hat` (latent policy, so T_θ can be
applied recursively; trained on σ=0 rows only). `fddad094` multi-step closed-loop
rollout, on the argument that at one branch point all candidates share z, so
`T_θ(z,u)` lies inside the function class of a head fed `(z, E_a(u))` — one-step
scoring cannot demonstrate a transition. Three v148 runs at 01:30-01:31.
`958f1f84` rewrote the world model (`train_v149_joint_wm.py`, 194 lines): joint
encoder/transition/heads, no reconstruction, two transition families (TD-MPC2-style
ensemble, Dreamer-style diagonal Gaussian). `ac0e51cb` scoped the prior negative
results to "a frozen alignment latent with MSE-to-exact-next-latent in three
separate stages" and added the literature rule to `CLAUDE.md`. `5583eec5` asserted
tapes share a latent definition (2048-d pre-proprio vs 2073-d) before pooling.
`68afbfb1` added the claim-discipline rules to `CLAUDE.md` — the "six conclusions
announced and withdrawn in one session" block. `f14e4474` ported a TD-MPC2-faithful
WM with the `no_trans` refuting arm *inside the same loop*. `a3fa5519` wrote
`train_v153_td_value_wm.py`, borrowing TD-MPC's representation learning but not
MPPI and not dense reward.

Five v149 run directories from 01:38-01:39 are **empty on disk**
(`ensemble_2026-08-29T063833Z/063920Z/063947Z`,
`stochastic_2026-08-29T063834Z/063921Z`) — started and killed. Two survive with
summaries (`ensemble_…064153Z`, `stochastic_…070308Z`).

**02:40 — a run that never finished.** `c9d53b04`: each TD step did three encodes
of 4644×2073, so 1500 epochs × 5 restarts never completed on CPU and the process
was reclaimed before writing any result. Minibatching was added.
`results/v153_td_value/2026-08-29T073934Z` is empty, consistent with that.

**03:48-03:53 — limitations and switches.** `982fa6a3` recorded that the
trajectories are deployment-collected but the **labels are not** (Δw from BDDL
predicates, success from env `is_success`), and that the 2048 candidate-level
labels came from deterministic replay — a simulator affordance. Also two
monitoring errors (a job launched with shell `&`, killed and unmonitored for a
day; twice concluding a live run had died from an unreliable `pgrep`).
`c179c78f` turned the two alternative explanations into **switches**
(`--encoder residual|frozen`, `--nstep`) rather than post-hoc stories, and
recorded jointly-trained 0.608 vs frozen-latent 0.637, bootstrapping advantage
+0.0069 CI [−0.0045, +0.0155] at n = 5.

**≈04:00-15:29 — a long gap in the commit record.** No commits. Nothing on disk
dates to this window. Whether this was sleep, a long run, or unrecorded work is
not recoverable.

**15:29 — the switch that was a no-op.** `aac738fa`: `--nstep` had been added but
never wired into the TD target, so it did nothing — "a switch that is not
implemented is not a prepared test." Same commit records the n = 5 advantage of
+0.0066 becoming **−0.0102 over 20 restarts** (last 16 averaging −0.0154), with
the bootstrap CI excluding zero on the negative side while the sign test is 9/21
(p = 0.66) — the two summaries disagree, so the effect is not robust in either
direction. On disk: `results/v153_td_value/2026-08-29T085255Z/summary.json`.

**15:47 — the five isolations.** `a2ab4460` recorded that across frozen vs
learned latents, one-step vs multi-step, MSE vs TD, offline vs deployed,
isolating the action-conditioned latent transition yields nothing measurable, and
named the structural reason (a critic needs no dynamics). It explicitly recorded
as **NOT licensed** any claim that latent world models are useless, and named the
untested use that would overturn it: using the transition to optimise action
sequences. On disk: `results/v153_td_value/2026-08-29T202919Z/summary.json`
(`encoder: frozen`, `nstep: 3`, `transitions: 4644`, `terminal: 576`,
`candidates: 2048`, `diff: −0.005466`).

**16:37-16:43 — two silent patch failures.** `6dfa52e0` "Save the world model:
v153 kept only AUC numbers, never the weights" — `best[k]` stored `(score, None)`
and nothing was serialised. `1a9c61d7` "Actually save the WM: the previous patch
applied only half" — `kept.append` landed but the `torch.save` block did not,
because the replacement anchor did not match and failed silently.
`results/v153_td_value/2026-08-29T213756Z` is empty, consistent with a run that
reported and wrote nothing. `e27f6481` (16:37) added
`train_v157_residual_actor.py`: policy improvement **in imagination**, with the
transition load-bearing by construction, `--no-imagination` registered in advance
as the refuting arm and the expected failure mode (the residual exploits model
error) stated in advance.

**16:39-16:50 — the probe that asked the wrong question.** `5476d383` added
`probe_v159_wm_quality.py`. `2deed2ba` recorded its result: ranking AUC and
predictive quality are **anti-correlated** here — the model selected for ranking
(frozen encoder, AUC 0.626) sits at 0.935× identity at h = 1 with a shuffled
action at 0.998×, value gap +0.188, V correlation 0.43; the model discarded for
ranking (mlp encoder, AUC 0.584) beats identity at every horizon (0.530× at h = 1
→ 0.299× at h = 8), value gap +0.021, V correlation 0.88-0.95. On disk the two
v159 summaries are terse — `results/v159_wm_quality/2026-08-29T214544Z` and
`…214546Z` record only `beats_identity_h1: true`, `uses_action_h1: true`, and the
`wm` path; **the quoted ratios are in the commit message, not in the JSON.**

**16:52-17:31 — the deployment, and the predicted failure realised.**
`3d95b749` deployed; `1c488734` recorded the result. All four are on disk in
`results/v163_residual_deploy/`, fresh 96-seed panel:

| arm | dir | successes / 96 | rate | env steps |
|---|---|---|---|---|
| base | `chain1b_lr2_base_2026-08-29T215228Z` | 44 | 0.4583 | 23,569 |
| residual, with imagination | `chain1b_lr2_imag_2026-08-29T220311Z` | 19 | 0.1979 | 23,884 |
| residual, without (refuting arm) | `chain1b_lr2_no_imag_2026-08-29T221543Z` | 15 | 0.1563 | 23,920 |
| residual, scale 0.01 | `chain1b_lr2_sc0.01_2026-08-29T223143Z` | 47 | 0.4896 | 23,585 |

The commit message adds base-vs-imag `+3 −28, p < 0.0001` and the
transition contrast `+9 −5, p = 0.424`; the residual gained **+0.014 in
imagination and lost 0.26 in reality**, saturating 96% of its norm bound. Same
commit records two things the author got wrong: the v159 probe measured value
correlation on **real** trajectories only, so it could not see the
off-distribution region the residual optimises into; and chain1b measures
0.39-0.60, "middling rather than low", while chain3 at 0.031 had been closed for
a reason that no longer applied. `chain1b_lr2_sc0.03_2026-08-29T224414Z` (17:44)
is an **empty directory** — a fifth deployment arm that produced nothing.

**17:40 — the fix for the extrapolating value.** `3e577596`: v164 had shown the
residual saturating its norm bound at every scale (0.01/0.03/0.06) with imagined
gain growing linearly, i.e. V monotone along the residual direction with no
interior optimum. V became 5 heads on independently bootstrapped batches with a
pessimistic `min` objective, and the actor now reports ensemble spread at the
residual it found. Three v157 runs at 17:29-17:30.

**17:44 / 18:22 — chain3 latents.**
`results/v121_deploy_latents/chain3_lr2_2026-08-29T224421Z` (72,000 env steps) and
`…232256Z` (71,901) — the return to the low-success testbed.

**22:39-23:37 — the framework, written, reverted, cut, and rewritten.**
`6a3643f8` amended §5.1 and §5.2 of `framework_design.md` from measurement
(proprioception required in LCState: pooled prefix hidden gives +0.81% action
gain vs proprio-alone +11.78%, shuffled actions at 1.217× i.e. worse than not
predicting; a Gaussian variant collapsed to identity error 0.0004 with the
transition 5.5× worse than doing nothing; imagination bounded by value
extrapolation, scale 0.15 → 0.198, scale 0.01 → 0.490 against a 0.458 base).
**17 seconds later** `bb7c6d82` reverted it out of the binding document: "I
appended 21 to framework_design.md and committed before the user asked me to hold
it for review." The section moved to `plan_and_progress/PROPOSED_framework_21.md`,
unmerged. `2168b680` (22:55) cut it from seven clauses to two. `909a9048` (23:16)
**deleted `PROPOSED_framework_21.md` entirely** and wrote
`PROPOSED_framework_v2.md` from zero, on the grounds that sections 14-20 of the
old document were execution logs rather than design. `ec7f5498` (23:20) restored
the LOCKED contracts and the falsification table into v2, and added the
label-free/label-dependent split. `c14d2133` (23:37) rebuilt v2 around a
**language-conditioned, label-free reward**, on three measured counts: labels are
privileged, they are nearly absent on the target task (3% success ≈ 3 anchors per
96 episodes), and the residual optimised against the label-derived value gained
+0.014 imagined and lost 0.26 real. `framework_design.md` was left untouched;
`PROPOSED_framework_v2.md` still exists in `plan_and_progress/` today,
`PROPOSED_framework_21.md` does not.

**23:54-23:56 — the last bug of the day, and it is on disk.** `59b3f6ab`: "Add
`load_valuewm` for real; the earlier patch silently did not apply — **fourth**
silent string-replacement failure this session. Verified by import, not by
reading the diff." Then `8ac3cff0`: `argsort(argsort(x))` gives tied values
arbitrary distinct ranks, so a **constant** input ranked as a permutation and
correlated 0.64 with stage; `stage_reached` is heavily tied (182 of 191 chain3
failures share one value), so the primary statistic was contaminated too. The
two `results/v167_value_stratification/` summaries bracket the fix exactly:

| | `2026-08-30T045417Z` (before) | `2026-08-30T045608Z` (after) |
|---|---|---|
| `rho` | 0.06311 | 0.24660 |
| `rho_clock` | 0.69452 | **NaN** |
| `rho_label_shuffled` | −0.02835 | −0.00764 |
| `passed` | false | false |

(192 episodes, 92 successes / 100 failures, both runs.) `rho_clock` going to NaN
is the fix working: all failures run the full horizon, so the clock null is
genuinely undefined, and the 0.695 was manufactured by the tie handling.

### Environment interaction

Summing `env_steps` over every run directory whose UTC stamp falls in local day
2026-08-29 (CDT window 2026-08-29T05:00Z → 2026-08-30T05:00Z), across 32
non-empty run directories: **324,734 env steps**. Dominated by two chain3 latent
collections (72,000 + 71,901), four chain1b deployments (~23.5k each), and two
v141 branch deployments (~31.2k each). Eight run directories in that window are
empty (listed above) and contribute nothing. This total is computed here from
`summary.json` files; it is **not** a figure that was recorded on the day.

### What a commit log cannot recover

These are the gaps a reconstruction cannot close, and the orchestrator should
carry them forward with the reconstruction:

1. **The reasoning.** Why the v149 rebuild chose two transition families rather
   than one; why the frozen-encoder model was picked for ranking before v159
   showed it barely predicts; why chain3 latents were collected at 17:44 that
   evening. Commit messages state conclusions, not the deliberation.
2. **The user's instructions.** `bb7c6d82` says "before the user asked me to hold
   it for review" — the request itself, its wording and scope, is nowhere. The
   framework rewrite from 22:39 to 23:37 was plainly steered by someone; that
   steering is lost.
3. **What was considered and dropped without a commit.** `2168b680` cut seven
   clauses to two; the four dropped ones survive only as one line of summary.
4. **The ~04:00-15:29 gap.** Eleven and a half hours with no commit and no
   artifact. Unrecoverable.
5. **The five empty v149 directories and the empty `sc0.03` deployment.** Started
   and abandoned; why, and with what error, is not recorded.
6. **Whether numbers in commit messages were ever in an artifact.** The v159
   ratios (0.935×, 0.998×, 0.530×, 0.299×, value gaps, V correlations) and the
   v163 McNemar counts are in prose only; the v159 JSONs carry two booleans. A
   reader of the reconstruction must treat those as *reported*, not *stored*.
7. **Ordering within a minute.** Several commits are seconds apart
   (`6a3643f8` → `bb7c6d82`, 17 s); which edits preceded which decision inside
   those windows is not resolvable.

---
