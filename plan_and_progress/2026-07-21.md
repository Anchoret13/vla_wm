# Progress — 2026-07-21 (overnight session)

Executing framework v0.2 §13: hidden-state-first. Assignment for tonight:
(1) finish the bounded projection fix + selectivity rerun, (2) collect the
decision-rate sequential dataset, (3) run the §13.2 diversity measurement with route visualization: same
state, same task, N π0.5 samples — how different are the realized controls.

## Projection calibration — PASSED (bounded diagnostic closed)

`scripts/calibrate_projection.py` after the column-mirror fix
(`col = w-1-col`, `lcwm/seg_masks.py`): markers sit ON their objects for all
visible objects across tasks 0/2/8 (basket/cans/butter/cheese; moka pot +
frypan; both moka pots). Off-frame markers (ketchup/milk/OJ in task 0) are
consistent with objects only partially visible at the crop edge.
`results/feature_controls/projection_calibration.png`. Selectivity v3 rerun
with the calibrated projection: results below when done.

## PRE-REGISTERED: §13.2 diversity / route measurement (descriptive)

Question: from ONE snapshot with ONE task instruction, how
different are π0.5's N=16 sampled 50-step realizations — in action space, in
executed end-state, and in outcome?

Protocol: states = {t2@t0 (stove "button"), t0@t0 (pick), t8@t0 (failure
task)}; N=16, seed 7; each candidate executed OPEN-LOOP for 50 steps from a
restored snapshot (reset → set_state → OSC re-anchor, replay-validity
machinery). Recorded: eef route, final object positions, per-step success,
final BDDL predicate bits.

Reported stats (descriptive, no pass/fail — informs the σ-perturbation and
branch-budget [DEC]s, decision deferred to review): pairwise action-L2
(normalized space), end-eef dispersion, moved-object end-position dispersion,
predicate/success outcome counts. Visualization: routes projected through the
CALIBRATED camera onto the raw-oriented frame, colored by outcome.

Caveat pre-declared: open-loop execution of a 50-step chunk exceeds the c=10
commitment the policy would actually use; route divergence is therefore an
UPPER bound on committed-execution divergence (the 10-step prefix statistics
are also reported).

## Sequential dataset (framework §9 source 1) — COLLECTED

`lcwm/seq_data.py`: 100 shards (10 tasks × 10 replay-valid demos), 3.9 GB at
`../datasets/seq_libero_10/` + manifest. Per decision step (c=10): 3 feature
streams, e_lang, q, obj_pos, action_block (10,7), per-atom BDDL predicate bits
(via the problem env's own `_eval_predicate`), success flag; per demo:
first_success_t, terminal_t. The Candidate-A instrument's training data is ready.

## Selectivity v4 (off-frame-aware masks) — honest status

v3's distractor reading (0.676 at t=0) was an artifact: all three distractors
project OFF-frame at t=0, and their edge-clipped disks sampled background/sink
tokens. v4 excludes off-frame objects and records the exclusions.

| t | t0_targets | t1_targets | basket | distractors | rest |
|---|---|---|---|---|---|
| 0 | 0.510 | **0.838** | 0.266 | n/a (all off-frame) | 0.339 |
| 80 | 0.174 | **0.316** | 0.215 | 0.204 | 0.285 |
| 160 | 0.416 | **0.740** | 0.178 | 0.490 | 0.346 |

Status: **directionally supportive** — t1_targets (the swapped task's NEW
targets) is the top group at every timestep — but the earlier "30×" claim is
dead (mask artifact), and the only timestep with a valid distractor control
(t=80) shows a mild contrast (0.316 vs 0.204). Object-role selectivity remains
OPEN pending disjoint object-sized masks / more states; it is a bounded
diagnostic, not a training gate (v0.2).

## §13.2 route measurement — RESULT: near-deterministic outcomes at init states

Figures `results/route_viz/routes_task{2,0,8}.png` (all three visually
verified: routes projected correctly, task-2 bundle terminates ON the stove knob; task-0 all 16 samples choose the SAME first
subgoal, the alphabet soup). Stats (`results/route_viz/stats.json`):

| state | action L2 (50 / first10) | end-eef disp | t10 disp | outcomes |
|---|---|---|---|---|
| t2@t0 stove | 1.73 / 0.62 | **0.70 cm** | 0.14 cm | 0 predicate flips |
| t0@t0 pick | 1.26 / 0.54 | **0.21 cm** | 0.12 cm | 0 |
| t8@t0 pots | 1.04 / 0.45 | **0.25 cm** | 0.09 cm | 0 |

**Finding: π0.5's noise-driven action diversity (~10% relative L2) washes out
to SUB-CENTIMETER end-effector dispersion under 50-step open-loop execution at
these initial states.** The proposal distribution is effectively ONE route ×16 here.

Pre-declared caveats apply: 3 init states only; 50 steps ≈ approach phase; no
mid-episode / contact-rich / failure states yet. Implications for the [DEC]s:
- At init states there is nothing for a reranker to choose between — proposal
  diversity must come from σ-perturbation tiers, OR reranking value must
  concentrate at states where futures genuinely diverge (contact/failure
  moments). Next measurement: repeat at mid-episode snapshots (incl. t6/t8/t9
  failure states) before fixing the σ [DEC] and branch budget.
- Branch collection from init states alone would supervise dynamics on nearly
  identical futures — stratification by phase is mandatory (as framework §9
  already requires).
