# 2026-08-06 — V7.3 execution record (contract in 2026-08-03.md)

Single daily execution record for 2026-08-06 (America/Chicago); the
governing preregistered contract remains the V7.3 continuation in
[2026-08-03.md](2026-08-03.md); prior execution records in
[2026-08-04.md](2026-08-04.md). Result roots created today carry the
2026-08-06 prefix. Standing discipline: training-path stages are
"EXECUTED — pending review" until their adversarial review findings
are fixed and re-verified.

Carried status: V7.3A COMPLETE (review-closed); V7.3B COMPLETE
(collection done; support masks frozen: milestone family only
t2|train, rank-reversal masked project-wide, recterm contrast all
cells, terminal-success variance only at t2_s2410_d28).

---

## V7.3C implementation bindings (registered BEFORE the run)

Anchor universe (task→source→anchor balanced before branch/horizon/
pair/language expansion): the V7.2 repaired union's 96 anchors
(existing schedule + caches reused), the 18 RELEASED V7.2 teacher
anchors (canonical semantics from their stored per-branch
valid_seq_canon; outcomes released per the V7.3 contract), and the
33 V7.3B acquisition anchors (dual-goal valid_seq; 2 GoalSpecs × 3
texts crossing; recovery/recterm outcome rows).

Model: ONE LCWM from `2026-08-03_v072_lcwm_loss_r1/
shared_initialization.pt` (never a V7.2 final arm). Existing V0.6
heads only. Bundles:
  L_cl    BYOL recurrent-EMA closure (V7.2B repaired mechanism);
  L_phys  d_q/d_obj absolute + within-anchor centered;
  L_task  outcome-tuple heads (valid_bits/flips per-goal where
          supported; p_valid + q@h from continuations; terminal
          success and damage ONLY where the support table shows
          label variance — per-task masks from
          2026-08-04_v073_data_r1/outcome_support.json + released
          teacher outcomes; tau_next remains masked); NO scalar rank
          head in any teacher-facing path;
  L_inv   language contract (same-goal paraphrase alignment +
          shared-physics across compatible goals, V7.2B mechanism).
Gradient balance per the contract: 20 source-balanced calibration
microbatches at the shared init (4/task; every bundle active in ≥10
microbatches and ≥1/task), g_k = median state-grad norm,
λ_k = g_geo/g_k frozen before the first update, scaled medians
asserted within 3×. Optimizer frozen: AdamW 3e-4/1e-4, 25 epochs,
clip 1.0, TBPTT 16, EMA 0.995, seed 0, final-step checkpoint only.
One (goal, text) query per anchor visit, rotation by (epoch+index).
Tensor-only; h-features per (obs, prompt) via extended cache with
alias reuse of every existing cache (identical (obs, prompt)
function). Review-first before training.

## V7.3C review cycle closed; training LAUNCHED

23-agent adversarial review → 16 confirmed (deduplicated 6
substantive): merged-schedule rotation left v072B anchors requesting
372 uncached (goal,text) keys; λ-calibration picked off-schedule
pairs (KeyError) and could not satisfy the registered bundle-activity
contract (inv 5/19, set size 19); (epoch+idx) rotation parity-locked
5 v073 anchors onto half their crossings — and the first fix
(stride 5) STILL aliased because the slot index drifts with the
epoch source rotation; crossed-goal outcome heads silently supervised
with canonical targets via a fallback; the contract's within-anchor
CENTERED outcome term was missing. Fixes: query choice decoupled
from slot index ((epoch + anchor-hash) mod len — consecutive
residues guarantee full coverage; VALIDATED empirically: 675 visits,
0 incomplete train anchors); builder enumerates v072B under the
merged schedule (index now 15,422 keys, 815 newly computed);
calibration drawn from scheduled visits with ≥2 inv-active per task
and exactly 20; cont.get(g) with no fallback; centered outcome term
added. 3-agent re-verification with empirical key-coverage check:
ALL GREEN (f1cc0b6 → current). Training launched (single model,
25 epochs, final-step checkpoint only).
