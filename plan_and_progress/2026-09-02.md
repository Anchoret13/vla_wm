# 2026-09-02 — Gate-0 authority closure review

This file continues the daily September log. No September monthly summary is
being maintained during active work.

## Starting state

The formal campaign remains pre-freeze and all target RGB remains untouched.
The September 1 `26/26` CPU result covered an intermediate candidate only; it is
not a Gate result or a final source closure.

## Confirm-authorization review: NO-GO

A read-only review found two remaining protocol blockers despite the green CPU
suite:

- the confirm authorizer validates the screen evaluation using the evaluation
  artifact's own embedded progress reference, rather than independently loading
  the frozen screen-progress and pre-oracle slots and closing their externally
  anchored `COMPLETE.json` chain;
- the screen expansion predicate accepts whatever metric-check dictionary is
  present when all of its values are true, rather than requiring the exact frozen
  check-name set and recomputing the complete visual subgate from sealed metrics,
  config and recovery/prefix inputs.

The repair requirement is therefore explicit: confirm authorization must close
`freeze -> screen RGB x2 -> progress -> separately published pre-oracle ->
evaluation`, then recompute the exact frozen combined-screen predicate before it
can authorize either confirm replay. Fully resealed self-consistent artifacts
must not be able to replace an externally anchored upstream stage. Formal
freeze and target execution remain **NO-GO** while these checks and the runtime
fingerprint/CUDA attestation are unresolved.

## Authority closure repaired

The two findings above and the later fail-before-I/O findings were repaired.
This records the final candidate; it does not retroactively turn the earlier
intermediate candidates into valid freezes.

- The confirm authorizer now independently consumes externally supplied hashes
  for the registration freeze, screen progress, separately published pre-oracle
  seal and `COMPLETE`, and screen evaluation. It closes the frozen
  `freeze -> screen RGB x2 -> progress -> pre-oracle -> evaluation` DAG, requires
  the exact frozen 19-name metric-check set, and recomputes the full visual
  subgate before publishing A.
- Confirm replay validates A and its complete F/P/S/Q/E ancestry before any
  `lexists`, `lstat`, `stat`, `open` or write below its target output slot.
- The progress producer identifies screen versus confirm from frozen paths using
  lexical operations only. Screen forbids A; confirm requires the external A
  hash and completes the registered replay's full A validation before its first
  RGB target operation. The evaluator enforces the same ordering before its
  first progress-artifact operation and requires the progress authority's A hash
  to equal the external A hash exactly.
- The producer no longer accepts or stores an injectable replay module.
  Publication reopens the anchored campaign, rederives phase, registrations,
  registration paths, semantic config, RGB roots and destination, fresh-loads
  the registered replay source, and uses that same local module for A/RGB
  revalidation and atomic publication. Module, destination, registration and
  config `dataclasses.replace` attacks now fail closed.
- The two-stage freezer remains no-clobber and recoverable: Stage B may resume
  only from an externally supplied hash of the published Stage-A
  `MATERIALIZATION_COMPLETE.json`; validate-only does not import the simulator or
  create an output.

Bottom-level negative tests trap `os.open`, `os.stat`, `os.lstat` and
`os.scandir`. Missing, malformed or incorrect A therefore fails with zero target
RGB/progress I/O. A final read-only cross-audit reported no remaining blocker or
High finding on these paths.

## Final mechanical verification

The final independent CPU command ran the RGB/authorization, progress/evaluator
and freezer suites together:

```text
39 passed, 1 warning in 16.28s
```

The split is 16 RGB/authorization tests, 18 progress/evaluator tests and five
fake freezer tests. The warning is the upstream `torch.jit.script` deprecation
warning; no test failed. `py_compile` passed, and
`flake8 --max-line-length=100` was clean over the complete touched Gate-0
source/test set.

Final candidate SHA-256 values:

- contract: `31fceb923028a1385a07c17fb607a29b8939dfff79d2d8a9c09769d02e006c6f`;
- freezer: `1806da6bf6c7e1a49168174774c30f6da92675b728a981511b576494aa621fe8`;
- RGB replay: `28dcc2300d1950a86f6cb548647a2931e1ba6eff3871e70f39e670b90007ff61`;
- confirm authorizer: `1b3e3226fb51212957ab1df36f2535ccb4fc1299009ff1018e02207c43979cb8`;
- progress producer: `68430834bacfa08a0368cfb652e11b8292769fd4606a1c251377a48a1774c2fc`;
- progress evaluator: `734858bec19c6c1b45cae3975702bc66831fe22b0ce913b8772fe7e8daf419fb`;
- SAM3 engine: `3f3f2c756728345b73d87d304ea7e5b9382c95572be1b607047c61ac906a45cd`;
- cream/SIFT engine: `23f43af8b6a496a7933adf86c12d8992db19a1ca7a4524ee8b98826cf43ccd01`;
- can and cream state machines:
  `403088c26b2edffd4465d2f17f06d326aa309d7beade6c98b2c5f2c4496e3bcd`
  and `55653cf7d1ad361a4caf76eaadaf9fa15c307a4d6111db2b4877d9e79a523a36`;
- semantic config: `6f40e1199e2ffe81c001e4acf341dad85af705783941f550f0c8d40729112267`;
- freezer, RGB and progress tests:
  `dcb2b3a902ddb840008d5989fcb440a8d51dc29591b2481211315bd4a11d4cf8`,
  `360e701a0085a148b93a166462c60a3a47572f534589abf45d1df6ed75d142c1`
  and `435e45faecde7a773b748200a4e00088eef06a9e910a8495556ff92ac993a158`.

## Formal execution remains NO-GO

Mechanical protocol closure is green, but the real campaign is not authorized.

- The host exposes an NVIDIA GeForce RTX 5090, driver `580.173.02`, compute
  capability `12.0`. The selected `vf0s` environment is PyTorch
  `2.11.0+cu128` / CUDA `12.8` / cuDNN `91900`, but reports CUDA unavailable and
  zero devices. Deterministic algorithms and cuDNN deterministic mode are off;
  cuDNN TF32 is on; `CUBLAS_WORKSPACE_CONFIG` is unset.
- The current registration fingerprint does not yet seal the GPU identity,
  driver, compute capability, cuDNN state, deterministic/TF32 flags or relevant
  environment variables. A fuller runtime attestation and a CUDA-visible formal
  environment are required before a real freeze.
- The supplied F/R/P/S/Q/E/A hashes must be recorded by a trusted, ordered,
  signed or WORM external ledger. SHA-256 bindings alone do not prove publisher
  identity, execution order or time. `created_utc` is metadata, not a trusted
  timestamp.
- The formal security boundary remains a fresh-process registered CLI. It trusts
  SHA-256, the registered evaluator's metric computation, the external ledger,
  Python/kernel/filesystem no-replace semantics, and excludes arbitrary
  same-process monkeypatching.

The real registration bundle, materialized-model root and formal campaign target
all remain absent. No real freeze, simulator construction/reset, target RGB
read/write, oracle access, Gate-0 pass or D1 interaction occurred on September 2.
